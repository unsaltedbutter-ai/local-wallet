# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
*.egg
uv.lock

# Virtual environments
.venv/
venv/
env/

# Test / tool caches
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Model weights (downloaded, pinned via models/MODELS.md)
models/*.gguf
models/models/
models/bin/

# runtime artifacts (local store, signer transfer dir, macOS)
localwallet.db
localwallet.db-wal
localwallet.db-shm
*.db
*.db-wal
*.db-shm
psbt-transfer/
.DS_Store

# user config (CFG-003: repo-root config.json — never commit user settings)
/config.json

# build-time commit stamp (VER-001: written by install.sh, regenerated per
# install — never tracked; its absence means an honest "source run")
/src/localwallet/_build_commit.py
