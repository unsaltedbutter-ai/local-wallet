# TASKS.md — ticket ledger (Phases 0–6 + security/mainnet hardening)

Source: PROJECT.md §12 Phase 0. Phase 0-flagged open questions (OQ1, OQ2, OQ5, OQ16, OQ20) land as ADRs per §14 / AGENTS.md.

**Phase 0 AC (PROJECT.md §12):** (1) "What's my balance?" returns a correct live testnet balance; (2) a malformed/nonsense model output is rejected cleanly (fuzz test); (3) the model runs fully locally.

**Standing invariants (AGENTS.md / PROJECT.md §5, §8):** mainnet-only (ADR-0021 supersedes the original "testnet-only until the Phase 6 gate" plan — testnet keys/addresses refused at parse+validation; network I/O only in `src/localwallet/chain/` (lint-enforced); the LLM emits intent envelopes only — 3-layer validation (GBNF grammar → pydantic → business rules) then allowlist dispatch; no `eval`, no codegen from model output; no secrets anywhere (watch-only); never log xpubs, addresses, or amounts.

| ID | Subsystem | Files | Depends on | Done when | Status |
|---|---|---|---|---|---|
| TCK-P0-001 | scaffolding | `pyproject.toml`, `.gitignore`, `README.md`, `src/localwallet/{agent,protocol,wallet,chain,tx,signer,node,store,ui}/__init__.py`, `src/localwallet/config.py`, `tools/lint_network.py`, `tests/`, `evals/`, `docs/adr/`, `models/.gitignore` | — | Layout matches §15; all Phase 0 deps pinned in pyproject (pydantic>=2, embit, httpx, llama-cpp-python, pytest, ruff — no other runtime deps); network-import lint (`tools/lint_network.py` + `tests/test_lint_network.py`): only `src/localwallet/chain/**` may import network modules (urllib, http.client, requests, httpx, socket, ssl, aiohttp); pytest collects cleanly | done |
| TCK-P0-002 | protocol | `src/localwallet/protocol/{__init__,envelope,intents,dispatcher,errors}.py`, `tests/test_protocol_*.py`, `docs/adr/0002-envelope-spec.md` | TCK-P0-001 | Envelope v0 pydantic schema: closed intent enum {respond, clarify, get_balance} + per-intent typed params + `v` version field; dispatch table = plain dict intent→handler, unknown intents rejected (no eval/codegen); typed error envelope + reject path; policy constants: exactly one re-prompt on validation failure, then `clarify`; ADR-0002 answers OQ2 minimally (versioning field, error envelope shape, no parallel intents in v0, params reference user entities inline) | done |
| TCK-P0-003 | chain | `src/localwallet/chain/{__init__,esplora,config}.py`, `tests/test_chain_*.py`, `docs/adr/0003-mvp-chain-backend.md`, `docs/adr/0004-testnet-choice.md` | TCK-P0-001 | Esplora client (httpx, sync): GET `/address/{addr}/txs`, `/address/{addr}/utxo`, `/blocks/tip`; balance-from-UTXO helper (confirmed+unconfirmed split); timeouts + bounded retry/backoff; base_url configurable, default `https://mempool.space/testnet4/api`; network lint passes (only chain/ imports network modules); ADR-0003 (OQ5: public mempool.space acceptable for Phases 0–3, honest-privacy caveat recorded) + ADR-0004 (OQ16: testnet4 chosen; rationale vs testnet3/signet) | done |
| TCK-P0-004 | agent-model | `models/download_model.py`, `models/MODELS.md`, `docs/adr/0001-model-runtime.md`, `docs/adr/0006-perf-budget.md`, `src/localwallet/agent/grammar/envelope.gbnf` | TCK-P0-001 | Pinned download script: Gemma 4 E2B GGUF + E4B fallback, URL+SHA-256 in `models/MODELS.md`, script verifies hash, resumable, refuses mismatch; ADR-0001 (OQ1: llama-cpp-python wheel chosen, llama-server binary documented as alternative); ADR-0006 (OQ20: perf targets TTFT/turn-latency for base Mac mini + mid-range Windows, measurement plan marked deferred-run); GBNF grammar matching envelope schema v0 | done |
| TCK-P0-005 | agent | `src/localwallet/agent/{runtime,loop,prompt,context}.py`, `tests/test_agent_*.py` | TCK-P0-002, TCK-P0-004 | llama-cpp-python integration (lazy import; unit tests skip cleanly if no local model file); grammar-constrained decoding wired to `envelope.gbnf`; 3-layer pipeline: grammar → pydantic (protocol layer) → business rules; exactly one re-prompt on validation failure, then `clarify` intent to user; context injector inserts tool results as structured blocks; system prompt encodes quote-verbatim + no-secrets rules; no network imports | done |
| TCK-P0-006 | ui+wiring | `src/localwallet/ui/cli.py`, `src/localwallet/wallet/zpub_stub.py`, `src/localwallet/app.py`, `tests/test_e2e_*.py` | TCK-P0-003, TCK-P0-005 | CLI chat loop end-to-end: user text → agent envelope → dispatcher → `get_balance` handler → minimal embit zpub/vpub parse + first-N receive-address derivation → chain UTXO scan → narrated balance; hardcoded testnet zpub via config (env/CLI flag; hard assert testnet, refuse mainnet keys in Phase 0); malformed model output takes the clean reject path; integration test marked network/manual | done |
| TCK-P0-007 | evals | `evals/golden/*.json` (≥10), `evals/redteam/README.md`, `evals/run_evals.py`, `tests/test_fuzz_*.py` | TCK-P0-006 | ≥10 golden prompts with expected envelopes (mix: respond, clarify, get_balance, ambiguous amounts/recipients); runner validates actual model output vs expected envelope (skips inference when no local model, still validates all fixtures through schema+business rules); fuzz test feeds malformed/nonsense/oversized outputs into the validation pipeline: clean rejection, no dispatch, no uncaught exception (AC #2). Red-team set is a documented placeholder for Phase 6 | done |
| TCK-P0-008 | agent-bridge | `src/localwallet/agent/remote_runtime.py`, `tools/lint_network.py`, `src/localwallet/app.py`, `evals/run_evals.py`, `tests/test_remote_runtime.py`, `tests/test_run_evals_selection.py`, `docs/adr/0007-temporary-openai-compat-runtime.md` | TCK-P0-005 | Same generate seam as ModelRuntime; env-selected remote OpenAI-compatible endpoint for debug; lint exception scoped to the one transport file; ADR-0007 records temporary status + privacy disclosure + no-eval-conclusions caveat | done |
| TCK-P0-009 | tools | `tools/envelope_playground.py`, `tests/test_envelope_playground.py` | TCK-P0-008 | Local-only (127.0.0.1) HTTP playground: type a hypothetical user statement → raw LLM output verbatim + validation verdict (status/intent/params/failures) via the real handle_raw path against a stub table; runtime selection mirrors app.py; display-only (no real dispatch, no chain I/O, no persistence in v0); privacy disclosure on-page when the bridge is active; capture-to-eval-fixture flow documented as follow-up | done |
| TCK-P1-001 | store | `src/localwallet/store/{__init__,db,models}.py`, `tests/test_store_*.py` | — | SQLite (WAL) schema v1: wallets, derivation state (per-branch max used index), addresses, utxos, transactions, sync cursor, settings; typed accessor layer (no raw SQL outside store/); foreign keys ON; versioned init via `PRAGMA user_version` (create-if-missing, safe re-open); context-managed connections; value-free error messages (no addresses/amounts in exceptions) | done |
| TCK-P1-002 | wallet | `src/localwallet/wallet/{descriptor,derivation,scan}.py` (supersedes `zpub_stub.py`), `tests/test_wallet_*.py`, `docs/adr/0008-script-types.md`, `docs/adr/0009-gap-policy.md`, `docs/adr/0010-single-wallet.md` | TCK-P1-001, TCK-P0-003 | Descriptor build (`wpkh([fp/84'/1'/0']vpub/{0,1}/*)`) + prefix→script/network detection (BIP84 default; P2SH-P2WPKH + legacy read; taproot decision = ADR-0008/OQ6); batched derivation receive+change; gap-limited scan (default 20, configurable, ADR-0009/OQ7) persisting per-branch max-used index + UTXOs/history to store; rescan command (full re-derive+scan, cache-invalidating) + stale-cache-fix simulation test; testnet gate enforced in parse_watch_key not just derivation (P0 SR carry-over); ADR-0010/OQ19 single-wallet profile v1 | done |
| TCK-P1-003 | protocol | `src/localwallet/protocol/envelope.py` (+intents get_history, get_utxos, new_address), `src/localwallet/agent/grammar/envelope.gbnf`, `src/localwallet/agent/prompt.py`, `tests/test_protocol.py`, agent test updates | TCK-P1-002 | Closed registry extended to 6 intents; schema+grammar+prompt updated TOGETHER (ADR-0002 cross-ref: grammar gains branches for new intents); params: get_history{limit?}, get_utxos{}, new_address{branch?} (final shapes decided in-ticket, documented); unknown-intent/malformed-params rejects tested; protocol security-review gate | done |
| TCK-P1-004 | app | `src/localwallet/app.py` (handlers + narration for history/utxos/new_address), `src/localwallet/ui/cli.py` (if needed), `tests/test_e2e_*.py` | TCK-P1-002, TCK-P1-003 | history/utxos/new-address flows end-to-end via store+scan; addresses quoted verbatim from tool output; new_address bumps derivation state in store; malformed model output still clean-rejects; privacy indicator intact | done |
| TCK-P1-005 | evals | `evals/golden/` additions (≥8 new cases covering new intents, ambiguity, negative cases), `tests/test_fuzz_validation.py` extension | TCK-P1-003 | New golden cases validate through schema+business rules; interim model-mode run against bridge recorded in TASKS.md; fuzz suite covers new intents | done |
| TCK-P1-006 | AC harness | `tests/test_phase1_ac.py`, `docs/phase1-ac.md` | TCK-P1-004 | Known-history testnet fixtures (incl. >20-address gap) scan → store matches fixture truth; rescan fixes simulated stale cache; env-gated live cross-check procedure vs mempool.space documented + runnable (Electrum manual step documented) | done |
| TCK-P2-002 | tx | `src/localwallet/tx/{__init__,dust,selection,psbt}.py`, `tests/test_tx_{dust,selection,psbt}.py`, `docs/adr/0012-tx-core-policy.md` | TCK-P1-001, TCK-P1-002 | Dust/min-relay computed from script size (Core v28 policy.cpp formula, canonical constants 546/540/294/330 verified, seeded property tests); deterministic coin selection (smallest-larger-first + single-coin improvement + no-shattering dust sweep, docstring-reproducible, exact integer-weight vsize verified against embit-built txs); unsigned PSBT with witness_utxo + bip32 derivations, structural validation, RBF sequence policy 0xfffffffd (N4, ADR-0012); change below dust folds into fee; InsufficientFundsError amounts documented as user-facing UI (ADR-0012) | done |
| TCK-P2-001 | chain | `src/localwallet/chain/{fees,price}.py`, `src/localwallet/chain/esplora.py` (+get_json), `src/localwallet/config.py`, `tests/test_chain_{fees,price}.py`, `docs/adr/0011-price-policy.md` | TCK-P0-003 | Fee estimator (3 targets, TTL cache, fail-closed shapes) + price oracle (TTL, staleness ladder, opt-out, rounding policy) over a generalized public `get_json` on EsploraClient; ADR-0011 (OQ4) | done |
| TCK-P2-003 | protocol+tx | `src/localwallet/protocol/{envelope,intents,__init__}.py`, `src/localwallet/agent/grammar/envelope.gbnf`, `src/localwallet/agent/prompt.py`, `src/localwallet/tx/flow.py`, `tests/test_{protocol,tx_flow,grammar_conformance,fuzz_validation,agent_prompt_context}.py`, `docs/adr/0013-confirm-gate.md`, `docs/adr/0002-envelope-spec.md` | TCK-P2-002 | create_tx/confirm_tx intents (registry 8; XOR amounts; strict bounds; bech32 layer-3); TxFlow state machine (IDLE→CREATED→CONFIRMED/CANCELLED/EXPIRED, TTL 600 s) + ConfirmGate (whitelist-exact; dual-key: envelope AND same-turn gate); ADR-0013 (OQ3); grammar lockstep incl. amount-pair alternation; security-review gate passed | done |
| TCK-P2-004 | app | `src/localwallet/app.py`, `tests/test_e2e_skeleton.py`, `docs/adr/0013-confirm-gate.md` | TCK-P2-001, TCK-P2-002, TCK-P2-003 | Send flow E2E: confirmation card (amount/recipient/fee/size/change/USD+rate age) → dual-key confirm (gate on raw utterance per turn + envelope) → CONFIRMED; DENY cancels; expiry; pending tx_ref in model FACTS (production confirm path); InsufficientFunds friendly line; security-review gate passed | done |
| TCK-P2-005 | evals | `evals/golden/golden-018..027.json`, `evals/redteam/confirm-bypass-001..006.json`, `evals/run_evals.py`, `evals/README.md`, `evals/redteam/README.md`, `tests/test_run_evals_selection.py`, `TASKS.md` | TCK-P2-003 | 27 golden (create_tx/confirm_tx/ambiguity/mainnet-recipient/standalone-confirmation) + 6 confirm-bypass redteam with negative matcher; golden-018 re-adjudicated per ADR-0013; interim bridge run recorded (golden 21/27, redteam 0/6 — structural gate holds) | done |
| TCK-P2-006 | AC | `tests/test_sparrow_ac.py`, `docs/sparrow-ac.md` | TCK-P2-002 | Deterministic fixture PSBT (byte-pinned or structure-pinned, documented), independent fee/change recomputation, env-gated artifact dump for manual import; Sparrow procedure doc with pass criteria | done |
| TCK-P3-001 | signer | `src/localwallet/signer/{__init__,base,file}.py`, `tests/test_signer_{base,file}.py`, `docs/adr/0014-airgap-file-conventions.md` | TCK-P2-002 | Signer interface; FilePsbtSigner export/import round-trip (base64 text, SHA256 sidecars, filename scheme per ADR-0014/OQ17), refuses unsigned/garbage/checksum-mismatch/overwrite | done |
| TCK-P3-002 | tx | `src/localwallet/tx/revalidate.py`, `tests/test_tx_revalidate.py` | TCK-P2-002 | Deterministic signed-PSBT re-validation: extract final tx, assert outputs/fee/inputs match intended PsbtMeta/PendingTx, signatures present; tampered-PSBT fixture caught deterministically (Phase 3 AC) | done |
| TCK-P3-003 | signer | `src/localwallet/signer/hwi.py`, `tests/test_signer_hwi.py`, `docs/device-notes.md` | TCK-P3-001 | HwiUsbSigner via hwi-as-library (lazy import; enumerate → fingerprint match → sign), device-absent/locked error mapping, mocked-hwi tests | done |
| TCK-P3-004 | protocol+chain | intents sign_tx/broadcast_tx/tx_status (protocol+grammar+prompt lockstep), TxFlow SIGNED/BROADCAST states, `src/localwallet/chain/esplora.py` (+broadcast POST /tx, GET /tx/:id status) | TCK-P3-002 | Registry 11 intents; flow extension behind ADR-0013 discipline; broadcast POST fail-closed with scrubbed errors; status-by-txid | done |
| TCK-P3-005 | app | `src/localwallet/app.py` (sign/broadcast/status wiring incl. file-signer lifecycle), `tests/test_e2e_skeleton.py` | TCK-P3-004, TCK-P3-002 | Full lifecycle offline E2E with mock signer (export→sign→import→revalidate→broadcast→status); §10 device-handoff narration; signed-PSBT mismatch hard-stops broadcast | done |
| TCK-P3-006 | AC | `tests/test_phase3_ac.py`, `docs/phase3-ac.md` | TCK-P3-005 | Tampered-PSBT deterministically caught; offline lifecycle AC; live real-device + on-chain broadcast procedure documented (literal device AC deferred-run: needs hardware) | done |
| TCK-P4-001 | node | `src/localwallet/node/{__init__,detect,doctor}.py`, `tests/test_node_*.py` | — | Local instance detection (Core RPC cookie/ports, mempool/electrs ports, IBD/sync state), doctor guidance content (Umbrel/Start9 easy mode, Core+prune minimal, self-hosted mempool for Esplora+explorer), advise-only (agent never runs privileged commands) | done |
| TCK-P4-002 | chain | backend abstraction behind the EsploraClient interface (self-hosted base_url via config), `src/localwallet/chain/` + `src/localwallet/config.py`, tests | TCK-P4-001 | Backend switch config-only; ALL address queries hit the configured instance (test double + integration test); no public-API calls when self-hosted | done |
| TCK-P4-003 | protocol+app | `node_status` intent (registry 12, schema+grammar+prompt lockstep), privacy-indicator state in banner/narration, `docs/adr/0017-node-recommendations.md` (OQ11), tests | TCK-P4-002 | node_status handler; indicator flips public-API ↔ own-node correctly; doctor content agent-narratable | done |
| TCK-P5-001 | chain+app | `watch_incoming` polling loop (configurable interval, test double), mempool time-since-block, intent (lockstep), tests | TCK-P4-003 | incoming mempool tx surfaced within one poll cycle | done |
| TCK-P5-002 | agent+app+store | confirmation ETA vs fee rate/mempool state; session memory/summarization (R13); transcript management (OQ14 scrub/export UX), tests | TCK-P5-001 | ETA sane vs live fees (deferred-run check documented); 30-min multi-topic session within context budget (simulated) | done |
| TCK-P6-001 | evals | eval expansion + full red-team (injection via chain data/tx labels, xpub-exfiltration attempts, destructive bypasses, fuzz depth); flip the ≥95% golden gate to ENFORCED in run_evals | TCK-P5-002 | gate enforced; red-team suite runs in CI-able pytest; results recorded | done (a63e24e: gate ENFORCED, 17 new redteam fixtures → registry 23, tests/test_run_evals_gate.py 30 pins, fuzz deep matrix; suite 1742/6; GGUF model-mode adjudication BLOCKED by GBNF-dialect finding → TCK-P6-004; OFFICIAL RECORD after TCK-P6-004+AGT-001: 26/53 = 49.1% (golden 21/30, redteam 5/23), exit 1 — redteam misses are the by-design structural-gate case; model-quality misses listed in HANDOFF §5 candidates |
| TCK-P6-002 | packaging | packaging decision + builds (pyinstaller/py2app, OQ9; signed/notarized macOS, Windows), model download UX (hash-pinned), Windows driver story (OQ10), tests where automatable | TCK-P6-001 | builds boot on clean base Mac mini + mid-range Windows laptop — DEFERRED-RUN (needs user hardware + Apple Developer account) | pending (RE-SCOPED 2026-09-08: distribution = TCK-DIST-001 install.sh + GitHub source — no signed builds needed; the .app/notarization path is OPTIONAL future work, MW-8 re-scoped accordingly; WEB-006 frozen-build item rides the same optional path) |
| TCK-P6-004 | evals/grammar | `src/localwallet/agent/grammar/envelope.gbnf` (+drift-pin test), possibly `pyproject.toml` wheel pin | TCK-P6-001 | llama-cpp-python 0.3.35 GBNF parser rejects underscore rule names AND multi-line rule continuations — envelope.gbnf uses both, so model mode segfaults at generate-time grammar load (found by P6-001 adjudication attempt; bridge runs never caught it because the MLX server ignores grammars). Fix the grammar dialect (dash-only names, single-line rules) or pin a wheel whose parser accepts it; upgrade tests/test_grammar_conformance.py drift pin to assert a REAL parse at generate time (LlamaGrammar.from_string is a no-op holder in 0.3.35), not just from_string; then rerun the enforced-gate model-mode adjudication on the pinned GGUF and record results | done (db820fe + runtime fixes in 46e80d9: grammar translated to 0.3.35 dialect — dash-only rule names, single-line bodies, 0/90 verdict diffs; drift pin asserts REAL generate-time parse; official record 26/53 = 49.1% exit 1 — accepted) |
| TCK-AGT-001 | agent-runtime | `src/localwallet/agent/runtime.py` | TCK-P6-004 | DEFAULT_TEMPERATURE 1.0 → 0.2 (one knob; top_p/top_k/max_tokens/n_ctx untouched; constructor override preserved; docstring rationale: grammar-constrained JSON emission is a low-temperature task). Live evidence: send request escalated ("Sorry — …") while identical prompt passed 5/5 isolated at the same settings — per-turn sampling flake at temp 1.0. Gates green (1745/7, ruff, lint, fixture evals); model-mode rerun recorded 26/53 (49.1%), exit 1 — same miss set as the temp-1.0 run ⇒ misses are model-quality limits, not sampling noise (red-team "misses" are the documented by-design case: structural gate is the defense); probe 3/3 clean at 0.2 | done |
| TCK-CFG-002 | config | `src/localwallet/config.py`, `src/localwallet/app.py` (load order only — prefer merging the config-file load inside `Settings.from_env`; touch app.py only if unavoidable), `tests/test_config.py` (new — does not exist yet; CFG-001's row erroneously lists it), README env/config docs | TCK-ONB-002 (same file, ordered) + MW-4 lift for the app.py touch | USER FEATURE REQUEST (2026-09-07, during MW-4): user-editable config file (e.g. `~/.localwallet/config.json` or repo-local, decide in-ticket) so gap_limit can be set without an env var; fail-closed parse (malformed = startup refusal, value-free error); precedence env > config file > DB settings key > shipped default; scope: generic loader for env-exposed Settings scalars, gap_limit is the shipped/documented example; MUST reconcile the chain_base_url ladder (ONB-002) with the config-file rung — one documented ladder for every keyed setting; document alongside the LOCALWALLET_* env list; zero behavior change when absent | done (config file ~/.localwallet/config.json, merged inside Settings.from_env(config_path=None) so every construction site gets it; one 4-rung ladder env > file > stored > default documented in README incl. chain_base_url injection pattern preserved; fail-closed value-free refusal incl. unknown keys; app.py load-order-only exit-2 wrapper; 2149/7, evals exit 0) |
| TCK-P6-003 | gate | R-register sweep (R1–R15 resolved-or-owned with named owners), privacy copy audit (§9 wording), mainnet-gate decision ADR (testnet→mainnet flip) | TCK-P6-002 | every AC green or deferred with owner; mainnet decision recorded | pending |
| TCK-SEC-001 | agent/transcript | `src/localwallet/agent/session.py`, `tests/test_session_transcript.py`, `docs/adr/0020…` (amendment) | — | export redaction covers JSON amounts, 64-hex txids (ordered before legacy-addr), BIP39-shaped seeds; realistic-params test blind spot fixed; full suite green | done (55f8d6d) |

| TCK-SEC-002 | wallet/scan | `src/localwallet/wallet/scan.py`, `tests/test_wallet_scan.py`, `docs/adr/0009…` (amendment) | — | absolute per-branch walk ceiling + explicit truncation flag; request budget bounded; normal gap semantics unchanged; full suite green | done (f4d27b8 + ce8603b) |

| TCK-SEC-003 | tools/lint | `tools/lint_network.py`, `tests/test_lint_network.py` | — | ban-list covers asyncio-outbound/poplib/imaplib/xmlrpc/multiprocessing.connection; dynamic-import detection; unparseable files fail loud; new bans test-pinned; lint exit 0 | done (b582ab1) |

| TCK-SEC-004 | money-path hardening | `src/localwallet/chain/esplora.py`, `src/localwallet/agent/loop.py`, `src/localwallet/signer/file.py`, `src/localwallet/app.py` + their tests | TCK-SEC-002 (+ designer banner copy) | broadcast txid bound to sha256d(tx_hex); envelope re-injection sanitized like other injections; PSBT file size-checked before read; card renderer never fabricates 0; banner copy per designer strings; security-review gate passed | done (41dd321; passed TCK-SEC-006 retro review; M1 follow-up closed by TCK-SEC-007) |


| TCK-SEC-005 | docs/ledger | `HANDOFF.md` | TCK-SEC-001..004 | HANDOFF refreshed: registry 12, three lint exceptions, uv.lock resolved, SEC sweep outcomes recorded | merged into TCK-DOC-002 |

| TCK-DOC-001 | docs/ledgers | `MANUAL-WORK.md`, `TASKS.md`, `.gitignore`, `uv.lock` | — | this row's deliverables exist | done (5202297: MANUAL-WORK.md + uv.lock housekeeping) |

| TCK-MAIN-001..003 | wallet → protocol/chain/signer/node → tx/app/evals/AC | (retroactive row — see commits e353aec, 861ca75, 7d98854) | — | Mainnet-only flip per ADR-0021: descriptor/derivation gates inverted, coin-type 0', private-key refusals strengthened; bc1 recipients, mempool.space mainnet defaults, Chain.MAIN signer default, RPC 8332; bc1 change gate + mainnet fixtures everywhere + AC docs flipped; suite 1677 passed / 6 skipped; evals 30/30 + 6/6 | done (retro review tracked by TCK-SEC-006) |
| TCK-MODELS-002 | models | `models/manifest.json`, `models/MODELS.md`, `docs/adr/0001-model-runtime.md` | — | E4B manifest entry verified already correct (orchestrator's initial "E4B → 404" claim was a misread — retracted); add `gemma-4-E2B-it-qat-q4_0` entry (ungated google QAT repo, file `gemma-4-E2B_q4_0-it.gguf`) as the R12 quant-comparison eval subject; correct MODELS.md (duplicated bootstrap snippet, stale "gated google" claim); ADR-0001 amendment note; live-verify all three manifest URLs through download_model.py's own request path (200 + GGUF magic + Range 206); download_model.py unchanged | done (QAT entry added; all 3 manifest URLs live-probed 200/206+GGUF magic) |

| TCK-DOC-002 | docs/ledgers | `MANUAL-WORK.md`, `HANDOFF.md`, `pyproject.toml` (description line only) | TCK-MODELS-002 (wording) | MANUAL-WORK: MW-3 marked done (2026-09-06, user funded MAINNET wallet), MW-2 reflects verified-working sources, all MW wording mainnet-consistent; HANDOFF refreshed (absorbs TCK-SEC-005): registry 12, lint exceptions, uv.lock housekeeping, SEC sweep + MAIN flip outcomes, next = MW-2 → TCK-P6-001; pyproject description mainnet-honest | done |

| TCK-SEC-006 | review (read-only) | none — diff range `df0ef64^..HEAD` | — | Retroactive security review of MW2-FIX/SEC-002b/SEC-004/MAIN-001..003 | done 2026-09-06 (verdict FIX-REQUIRED: MAIN flip complete, txid binding/sanitization/card/banner PASS; M1 sidecar gap → TCK-SEC-007; L1/L2 downloader wording → TCK-MODELS-003; L3–L5 follow-up register) |
| TCK-UX-001 (ABSORBED by TCK-SCAN-003 fecb73f — dots through emitter, prompt-live ordering, value-free pins all landed there; row kept for provenance) | ui+wallet | `src/localwallet/app.py` (startup order in `main`/`_startup_scan`, dot printer), `src/localwallet/wallet/scan.py` (optional per-probe `progress_fn` callback), `tests/test_e2e_skeleton.py` + `tests/test_wallet_scan.py` | — | Live-run findings 2026-09-06: (1) "Type a message" prints before the multi-minute startup scan — move it after; emit "Checking for new transactions" before scanning; (2) dot-per-address-probed progress on one wrapping line (flushed, newline + existing summary line at completion) — dots ONLY, no addresses/amounts (value-free); scan_wallet takes optional progress_fn (default None = current behavior; watch probe + tests unaffected, silent); --rescan shows dots too; (3) pinned tests: callback fires once per probed address for both branches, args carry no address/amount values, None-callback behavior unchanged | pending |
| TCK-CFG-001 | config+wallet | `src/localwallet/config.py`, `src/localwallet/app.py` (pass-through), `src/localwallet/wallet/scan.py` (precedence in `_resolve_gap_limit`), `tests/test_wallet_scan.py` (gap_limit coverage lives here; tests/test_config.py does not exist), README/HANDOFF env-var list | TCK-UX-001 (same files, ordered) | `LOCALWALLET_GAP_LIMIT` env → Settings.gap_limit (str in Settings, parsed fail-closed 1..1000 per ADR-0009; malformed = startup refusal, value-free) → precedence explicit call arg > env > DB settings key > DEFAULT_GAP_LIMIT (20, shipped default unchanged); dev use: LOCALWALLET_GAP_LIMIT=2 for fast scans; ADR-0009 unchanged (rescan semantics, out-of-window detection still honest with small gaps) | done |
| TCK-SCAN-001 | wallet/chain | `src/localwallet/wallet/scan.py`, `tests/test_wallet_scan.py` | TCK-CFG-001 (same file, ordered) | Skip the `/address/{a}/utxo` fetch for addresses whose `/txs` walk result was empty (no txs ⇒ no UTXO): fresh-wallet startup 81→41 calls; persist/reconcile semantics unchanged; pin call-count expectations on the MockTransport tests | done (fresh wallet 81→41 calls pinned; funded case 43; persist/reconcile + ceiling/progress_fn unchanged; fixture-truthfulness fix rippled to e2e/backend-switch/phase3 mocks — 53 tests served physically-impossible zero-history UTXOs; 1800 passed/7 skipped, all gates green) |
| TCK-SCAN-002 | design | `docs/adr/0022-scan-freshness.md` (docs-only; drafted TOGETHER with ADR-0024 so the threading model agrees) | TCK-SCAN-001 + TCK-WEB-000 decisions | ADR decides the non-blocking startup-scan model: cooperative chunked resume driven by the existing single-threaded tick loop (preserves ADR-0019 no-threads design) vs background thread + own WAL connection; cache-served answers during first scan carry a deterministic tool-owned freshness flag (narration-only — model never authors freshness claims; balances verbatim); per-intent policy on partial data: get_balance/get_history/get_utxos MAY answer stale-flagged, new_address MAY (chain-free), **create_tx MUST refuse until first scan completes**; drafts the TCK-SCAN-003 file plan | pending (REVISED per web-architecture consult: chain I/O runs on a dedicated worker with NO store access returning immutable records; ONLY the engine thread persists via persist_scan_result's single atomic tx — background-worker model now load-bearing, covers watch polls too; freshness flag + create_tx-refuses-pre-first-scan survive unchanged; ADR-0019 §2 no-poller-thread test pin retired as PLANNED work) | done (ADR-0022 reconciled to the consult revision: chain worker no-store-access + engine-thread persist_scan_result atomic tx, freshness key, per-intent policy, create_tx MUST-refuse pre-first-scan; SCAN-003 file plan drafted; NOTE for SCAN-003 implementer: no-poller-thread pin retirement attribution reconciled — WEB-001 already landed the engine-thread reality, re-scope the enumerate()==1 pin to the thread delta) |
| TCK-SCAN-003 | app+store | per ADR-0022: `src/localwallet/app.py`, `wallet/scan.py`, `store/`, `tests/`, evals stale-flag narration fixtures | TCK-SCAN-002 | REPL prompt live <1 s after launch; chunked scan finishes across ticks with dots between turns; stale-flagged balance/history answers; create_tx blocked pre-first-scan with friendly line; watcher per ADR; full suite + new pins | done (scan.py phase split plan/fetch/persist — fetch has NO Store param, frozen record sets; app.py ChainWorker + ScanFlow + StartupScan gate, freshness on balance/history/utxos + FACTS injection, create_tx dispatcher-owned refusal step-0, watch polls ride the worker, exit drain bounded 8s; prompt.py one FACTS rule; 23-test worker suite + pin re-scopes per ADR-0022 decision 7; evals golden-037 + stale-fabrication-001 (37/37+25/25+8/8 exit 0); 2021/7; security-review APPROVE — MEDIUM stale-plan persist race FIXED engine-side merge at persist time (anti-reissue floor, allocated-never-downgraded, mutation-proven pin), LOW skipped-state FACT fixed; deviations: ADR text rejected cooperative chunking in favor of worker model, --rescan non-blocking too) |
| TCK-ONB-001 | design | `docs/adr/0023-setup-backend-selection.md` | — | ADR: first-run backend choice (public Esplora sees all wallet addresses — ADR-0003 caveat). v1 scope: **Esplora-protocol URLs** (self-hosted mempool.space/Esplora incl. Umbrel/Start9, rides the ADR-0018 switch) + explicit opt-in prompt; default stays public; Electrum-protocol + raw Core-RPC named as future work (TCK-ONB-004); designer sign-off on setup/privacy copy (§9) | done (ADR-0023 written + revised per user UX direction 2026-09-07: 5-step onboarding conversation — greeting/zpub ask → non-blocking node ask post-xpub (user's privacy pitch, v1 scope caveat) → load narration (ADR-0022/SCAN-003 dependency + interim blocking variant) → load-complete → backend setup branch; copy drafts pending §9 sign-off; ADR-0018 testnet4 clause noted superseded by ADR-0021) — §9 sign-off DONE 2026-09-07 (designer pass on docs/adr/0023: 4 steps edited, 4 signed off; implementation-time strings flagged in-file: syncing-node branch figures verbatim from doctor facts; step-3 main variant gated on TCK-SCAN-003) |
| TCK-ONB-002 | store+config | `src/localwallet/store/`, `config.py`, `tests/` | TCK-ONB-001 | Backend choice persisted in settings; precedence env LOCALWALLET_CHAIN_BASE_URL > stored > public default implemented as a pure resolution function in config.py (config.py stays stdlib-only — NO store import; caller/ONB-003 injects the stored value at startup; config.py:3 documents this constraint); ONB-002 tests persistence + precedence at the store/config layer — wiring the stored rung into app.py startup is ONB-003's job; ADR-0018 single selection point preserved; zero change when unset; migration + tests | done (store get/set_chain_base_url + pure resolve_chain_base_url in config.py, 25 new tests; no migration needed — settings table exists in schema v1; suite 1770/7, ruff, lint, fixture evals green) |
| TCK-ONB-003 | ui+app | `src/localwallet/app.py` first-run flow, `ui/cli.py`, `node/doctor` integration, tests | TCK-ONB-002 (+ TCK-SCAN-003 for the non-blocking load line — interim blocking variant mandatory per ADR-0023 §flow if SCAN-003 hasn't landed; NOTE: MW-4 done 2026-09-07 — hands-off lifted, dispatchable after SCAN-003) | Full onboarding conversation per ADR-0023 (greeting/zpub ask with seed-words clarification → skippable node ask post-xpub → load narration → load-complete → backend setup branch): public vs own node, URL entry + mainnet/IBD validation via chain/+node probes; 3-state privacy indicator set honestly from the choice; doctor guidance on failure; never silent public fallback | done (5-step flow per ADR-0023 §9-signed copy in new ui/onboarding.py (import-cycle deviation documented); trigger = CLI+interactive+no backend rung+fresh wallet; headless byte-identical skip; URL validation via new chain check_backend (strict tip parse + mainnet genesis proof, object shape canonical); nothing saved on failure, explicit public fallback only; scan-failure never narrates load-complete; web gets deterministic hint line only; 23 tests, 2177/7, evals exit 0, zero model contact pinned; security-review FIX-REQUIRED → both MEDIUMs fixed (check_backend collapses httpx.InvalidURL value-free — pump can't wedge, malformed live URL marks scan skipped; genesis proof reads the real /blocks/0 object shape — bare-hash tolerated); step-2 re-offer triggers deferred) |
| TCK-ONB-004 | chain (SCHEDULED 2026-09-09 — user priority) | `src/localwallet/chain/` (electrum.py, bitcoind.py or rpc.py adapter), `ui/onboarding.py`, `/setup` + web settings URL handling, tests, ADR-0018 amendment | TCK-ONB-005/006 + milestone plan (general dispatch drafts it) | USER DIRECTION (2026-09-09): support bitcoind + electrum server + mempool-compatible servers; accept ssl:// OR https:// URLs and AUTO-DETECT the kind (port heuristics + probe, never ask the user to classify); optional credentials (user/pass) with a "no credentials needed" checkbox (bitcoind RPC auth; electrum ssl usually none); /setup + web settings + watchkey-first-run flow all gain the credential fields. Milestone plan drafted by general dispatch before implementation: M1 electrum-protocol client (ssl, server.version/blockchain.scripthash.get_balance/history/utxo subs), M2 bitcoind RPC (scan via descriptor scanobjects or per-address RPCs — decide in plan), M3 auto-detect + creds UX. Each milestone: tests with local fixture servers, lint, review | done (M1 d03a4d1, M2 ca78020, M3 cdcff81 — COMPLETE per HANDOFF part 3; plan: docs/onb-004-backend-adapters-plan.md: M1 Electrum ssl → M2 bitcoind scantxoutset (watch-only descriptor scan; unspent-only history tradeoff documented) → M3 autodetect+creds; contract adds backend-native estimate_fee + supports_price capability — get_json is Esplora-only; 5 open questions w/ recommended defaults); M1 dispatch next || TCK-SEC-007 | signer | `src/localwallet/signer/file.py`, `tests/test_signer_file.py` | TCK-SEC-006 | SR-M1: size-gate the checksum sidecar (stat-before-read, bounded read) on the import_signed money path, and bound the pre-existing unbounded unsigned read on the export path (file.py:227); tests plant oversized sidecar/unsigned files and assert refusal, value-free; full suite green; re-review of fix diff APPROVE | done (re-review APPROVE, M1 CLOSED; suite 1682 passed / 6 skipped) |

| TCK-MODELS-003 | models | `models/download_model.py` (docstring/argparse help text ONLY — no logic changes), `models/MODELS.md` | TCK-SEC-006 | SR-L1: fix the false docstring claim that urllib strips Authorization on cross-host redirects (state actual behavior + why it is still acceptable); SR-L2: docs/help prefer `HF_TOKEN` env over `--hf-token` argv (process-table/shell-history exposure) | done (SR-L1/L2 wording; download_model.py logic untouched) |

| TCK-HW-001 | signer | `src/localwallet/signer/hwi.py`, `tests/test_signer_hwi.py`, `pyproject.toml` (+`uv.lock`), `docs/adr/0015` (amendment note) | — | Jade locked-at-sign failure (MW-4 live, 2026-09-07): root cause = `requests` absent from .venv → hwilib jade `HAS_NETWORKING=False` → `JadeClient.__init__` raises DeviceNotReadyError('Use "Recovery Phrase Login" or "QR PIN Unlock"…') BEFORE any auth_user; our enumerate_devices discards the swallowed `error`/`needs_pin_sent`/`needs_passphrase_sent` flags → generic wrong guidance (hwi.py:98). Fix per debugger handoff: add `requests` dep (hwi jade pinserver relay — blinded blobs, PIN never host-side; docstring sentence added; ADR-0015 amendment paragraph for signer-call-stack outbound HTTPS); preserve flags on DeviceInfo; branch guidance in _select_device by device class (Jade on-device unlock wording incl. menu escapes; needs_pin_sent=True → host-pin class note); map bare JadeError -32000 → canceled; do NOT call prompt_pin/send_pin for Jade (hwi 3.2.0 raises UnavailableActionError — devices/jade.py:499-514); app.py untouched (guidance flows verbatim); mocked tests per FakeCommands enumerate shape; known ceiling: locked Jade can block enumerate while user enters PIN (ponytail-comment the ceiling) | done (requests>=2.31 added, HAS_NETWORKING=True verified; class-branched guidance _MSG_JADE_LOCKED/_MSG_HOST_PIN; JadeError -32000→canceled guard; 6 new tests; suite 1776/7, ruff, lint exit 0, fixture evals exit 0; security-review APPROVE — 1 LOW guidance-ambiguity note, pre-existing class) |
| TCK-UX-002 | tx+app+evals | per docs/ux-tx-card-feedback.md (3 sub-tickets CARD-BRIEF / FLOW-REQUOTE / GATE-MERGE) | TCK-HW-001 not required — schedule post-MW-4 (app.py hands-off) | Designer-approved tx-card redesign 2026-09-07 (user live-run feedback): brief default card (fee+ETA one line w/ chain/eta.py hedge verbatim; size/inputs/change/expiry/ref behind /details); one code-owned options line ("faster"/"slower"/"/details") replacing model-spilled menu (fee tweaks while pending were free-but-unexecutable options); approve/sign MERGE — "sign" joins gate CONFIRM whitelist, dispatcher chains confirm→device-handoff in one turn (ADR-0013 §3 "no signing gate" already holds: device screen is trust anchor; broadcast stays separately gated); FLOW-REQUOTE (dispatcher-owned replacement from CREATED, commit-only-on-success, new tx_ref/TTL) ships with ADR-0013 amendment; fee-speed via existing create_tx.fee_target — NO new intents/schema changes; eval fixture "sign while CREATED ⇒ confirm_tx" | done (card To/Pay/Fee/From + conditional slot, /details cached reprint CREATED-gated; FLOW-REQUOTE replacement commit-only-on-success, old ref inert, fresh TTL; GATE-MERGE "sign" in whitelist + chained confirm→handoff, zero whitelist collisions pinned; golden-031..034 + confirm-bypass-007; prompt: never-guess-fee_target + HW-004 signer-advisory line; 1845 passed/7 skipped, ruff, lint exit 0, evals 34/34+24/24 exit 0; security-review APPROVE — LOW: refused-create burns a change index (pre-existing window, comment nit); INFO: silent-confirm only if a future transport bypasses _run_turn — ADR-0024 already forbids) | superseded-notes (design READY + MW-4 LIFTED 2026-09-07 — dispatchable: docs/ux-tx-card-feedback.md finalized 2026-09-07 after provenance correction — no model spill exists; primary requirement = proactive speed/importance offer when create_tx omitted params.fee_target; brief card + approve/sign merge + FLOW-REQUOTE mechanics stable; key implementation notes: fee_target_defaulted display key needed (app.py:1257 collapses omitted→MEDIUM), offer must be a wh-question (yes/no are gate words), zero whitelist additions) |

| TCK-HW-002 | signer+app | `src/localwallet/signer/hwi.py`, `src/localwallet/app.py` (ONLY: `_run_turn` retry interception), `tests/test_signer_hwi.py`, `tests/test_e2e_skeleton.py`, `docs/adr/0015` (amendment) | TCK-HW-001 | MW-4 live blocker (debugger root-cause 2026-09-07): fingerprint gate compares account-key fp (zpub m/84'/0'/0', app.py:725/2293/1688) vs device MASTER fp (hwilib jade.py:549 get_master_fingerprint, user's Jade shows 40dbb192) — can never match for any account-level zpub; descriptor.py:355-360 already notes OQ18 device registration unwired. Fix option B (orchestrator decision: smallest live-AC unlock; ADR-0015 trust anchor re-framed, revalidate stays the binding gate): signer verifies device controls the ACCOUNT key — derive/compare device fingerprint at the descriptor account path vs descriptor origin fp; IMPLEMENTER MUST FIRST verify hwi 3.2.0 JadeClient exposes an account-path fingerprint (get_pubkey_at_path/retrieve_xpub) — if NOT, STOP and report, do not improvise. Part 2: bare 'retry' is narration-instructed but never routed (app.py:2753-2808 sends it to the LLM → 'Please try your request again') — deterministic interception in _run_turn gated on flow.state CONFIRMED re-invoking sign_tx handler, never via model; no other app.py changes. Tests: mocked unlocked-Jade repro (fingerprint=master, expected=account → now passes), app-level retry-at-CONFIRMED pin, retry outside CONFIRMED unchanged | done (a432853: account-fp binding via get_pubkey_at_path + hash160[:4] vs descriptor origin fp, fail-closed, _injected skip removed, TOCTOU on open client; code-owned retry Envelope at CONFIRMED only; 1782 passed/7 skipped, ruff, lint exit 0, evals exit 0; security-review APPROVE — 1 LOW availability note: only bare 'retry' token matches; deviation: tests/test_phase3_ac.py 9-line fake-arity ripple) |
| TCK-UX-003 | ui+app+store | per docs/ux-tx-card-feedback.md §5 (provenance /details view) | TCK-UX-002 (same doc/flow) | User request 2026-09-07: when user asks for more info on a proposed tx, explain where the UTXOs come from — arrival (block_time/height join exists for confirmed; first-seen timestamp for unconfirmed = store work sub-item), own-scan address history ('other activity we've seen', honest bounds line), KYC/non-KYC = NO data source in v1 → user-assigned labels only (LABELS-USER future feature; '(unlabeled)' fallback; label text never enters model context — /details stays terminal-only). Per-source line format + §9/§10 conflict notes in doc §5; per-line USD needs new rate handler key (orchestrator call at implementation) | pending (backlog) |

| TCK-HW-004 | app+signer | `src/localwallet/app.py` (signer-selection line :1674 + stale comments :1593-1597), `src/localwallet/protocol/envelope.py` (comment wording only :465-467), `tests/test_e2e_skeleton.py` | — | MW-4 finding (debugger root-cause 2026-09-07): model's optional sign_tx `signer` param silently outranks user config (app.py:1674 `params.signer if not None else selection.kind`) — LOCALWALLET_SIGNER=file + attached Jade → device signed; no device → 'No device found' instead of file export. Fix per handoff: config authoritative, `kind = selection.kind`; model param advisory — on conflict include value-free guidance field ('using your configured file signer') in handler result, never refuse, never switch; update stale comments (app.py:1593-1597, envelope.py:465-467: 'handler/app decision, never model-chosen'); env matrix tests (unset/file+hwi-envelope→file export to LOCALWALLET_SIGNER_DIR, /hwi+omitted→hwi); docs/phase3-ac.md promise (LOCALWALLET_SIGNER=file) now honored. Sign path → security-review | done (config authoritative, conflict guidance value-free additive-only; 1798/7 at ticket close, 1800/7 combined tree; security-review APPROVE — LOW follow-up: prompt.py signer-advisory line + eval folded into TCK-UX-002; INFO: conflict note not narrated by _print_sign_tx on non-device_error paths — web UI reads guidance from result) |
| TCK-UTXO-001 | store+app | store schema (tx/coin-level tags + free-text note), `/label` command (app.py — post-MW-4, LIFTED) | TCK-UTXO-002 (selection consumes tags) | Per docs/ux-utxo-notes-design.md: closed tag set {kyc, exchange, p2p, purchase, consolidation} + one free-text note per coin; tags are user-authored facts consumed by DETERMINISTIC selection code, surfaced verbatim in narration, NEVER model context (§7.10 injection surface) and never model-authored; capture moment per doc §1 (post-broadcast/on-demand, never mid-confirmation) | done (store schema v2: separate outpoint-keyed coin_labels table — persist_scan_result's utxos rewrite can never touch it; multi-tag union canonical order, 500-char note, spent-coin retention, v1→v2 versioned migration fail-closed; /label terminal command + one-time post-broadcast capture hint + change-coin lineage at broadcast (post-success, self-contained); never-model-context pump-pinned; 27 tests, 2065/7; security-review APPROVE — 2 LOW (migration atomicity comment, lineage error wrapping) ride the UTXO-002 diff) |
| TCK-UTXO-002 | tx+config | `src/localwallet/tx/selection.py`, `src/localwallet/config.py`, store settings keys, tests | TCK-UTXO-001 (tags must exist to be consulted; fallback behavior when untagged = whole doc §2) | Selection policy per doc §2 (ADR-0012 amendment draft in doc): run existing deterministic algorithm over kyc-side pool / other-side pool / full set — pure pool wins whenever it finalizes, mixed only when no pure pool funds (mandatory card warning); consolidation: fee ≤ consolidate_below_sat_vb folds unselected coins below target min (bounded ≤4 inputs, each ≥2× own incremental fee), narrated on From line; single-coin improvement never shatters a coin above target max; settings utxo_target_min_sats=100000 / utxo_target_max_sats=10000000 / consolidate_below_sat_vb=2, gap_limit-style ladder (fail-closed, env > stored > default, min≥max refused); pure-pool-may-cost-more is BY DESIGN (amendment says so) | done (selection policy layers wrap untouched core: pool partition kyc-side/other-side/full with doc-§1.4 table, pure wins by (fee, other-tiebreak), mixed flag for card warning; consolidation ≤4 inputs w/ 2× incremental-fee guard; 3 settings w/ gap_limit-style fail-closed ladder + min≥max refused at ladder AND select_coins; store LOW items from UTXO-001 review closed (lineage error wrap + truthful migration comment); 70 new tests, 2135/7, evals exit 0; security-review APPROVE — 2 LOW availability foot-guns in settings clear/write path (env-blind pair check) → fold into UTXO-003 wiring; app.py wiring + narration = TCK-UTXO-004 per doc §5) |
| TCK-UTXO-003 | app | `/settings` interaction (app.py — MW-4 lifted) | TCK-UTXO-002 | Advanced settings surface per doc §3: list current values + change flow, plain-UI rules, data model UI-agnostic (web settings page reads same keys later) | pending |
| TCK-UTXO-004 | evals+narration | eval fixtures, narration strings (renderer half MW-4-lifted) | TCK-UTXO-002 | Narration/eval fixtures per doc §4: re-quote must not silently change tag-mix (or re-narrates), consolidation line on card, mixing warning, fixtures authorable early | pending |

| TCK-WEB-000 | design | `docs/adr/0024-web-ui.md`, `docs/adr/0019` (amendment), `tools/lint_network.py` + test pins (exception list only — may land with WEB-001) | — (decisions fixed by orchestrator + consult 2026-09-07) | ADR-0024 records: localhost-only opt-in web UI (CLI stays default); stdlib ThreadingHTTPServer HTTP/1.0 (stay-and-document; no new runtime deps); ONE engine thread owns ALL state — store constructed ON engine thread (sqlite check_same_thread), turns NEVER cancelled (complete + event replay), all reads through engine queue; chain I/O on a dedicated worker with NO store access → engine persists (persist_scan_result atomic tx); SSE via fetch+ReadableStream (NOT EventSource — token can't ride headers), event-id ring buffer + Last-Event-ID replay, bounded per-connection queues (overflow→close→replay), heartbeat, broken-pipe swallow, sentinel shutdown (block_on_close), one frame per write + TCP_NODELAY; security: 127.0.0.1 bind + ephemeral port + canonical URL, random per-launch token header, Host allowlist (DNS-rebinding primary), NO CORS headers, NO cookies ever (CSRF moot — documented so nobody adds sessions), token never in URLs/logs, access log suppressed, local-process trust statement; XSS render contract: textContent-only + CSP + model-output red-team render fixtures (sanitize_tool_output is not an HTML escaper); buttons = canonical whitelisted utterance through FULL _run_turn (endpoint→flow.confirm() bypass forbidden — ADR-0013 dual-key); ADR-0019 amendment (single state-owning engine thread + stateless transport + chain worker; retire no-poller-thread test pin as planned work); lint exception scope for ui/web server files (inbound loopback ≠ outbound, ADR-0016 structure); launch --web/LOCALWALLET_UI=web; packaging prereqs (stdout-dots parameterization, importlib.resources static) | pending |
| TCK-WEB-001 | engine+app | `src/localwallet/app.py` (queue-driven pump replacing _repl input_fn; output_fn→event emitter; scan-dot stdout parameterization — TCK-UX-001 surface), `src/localwallet/ui/web/` skeleton, lint exception + pins, tests | TCK-WEB-000 | Engine thread + command queue + futures-with-timeout pump; wiring (Store, lazy Llama) constructed on engine thread; existing ~96 REPL e2e tests pass UNEDITED against a queue-driven harness (seam proof); scan progress dots flow through output_fn | pending (DESIGN POINTS PINNED by orchestrator 2026-09-07: lint constant WEB_SERVER_DIRS mirroring NODE_NETWORK_DIRS; CLI world KEEPS the between-turns tick unchanged — chain worker is web-world only; freshness result key = freshness ('fresh'|'stale'); render red-team fixtures in evals/redteam/render/; ADR-0019 amendment lands with this ticket incl. tests/test_watch_incoming.py pin retirement) | done (EngineEvent/EventEmitter/QUIT/start_engine+EngineHandle pump API; _repl rewritten as CLI transport over the pump, bootstrap-on-engine-thread incl. Store; dots parameterized through emitter; WEB_SERVER_DIRS lint exception + 4 pins; ADR-0019 amendment landed, watch thread pin → thread-delta; autouse conftest queue harness proves ~96 e2e tests pass UNEDITED; 10 new pump pins; 1859 passed/7 skipped, ruff, lint, evals green. Deviations: stdin-feeder daemon thread in CLI mode w/ ordering handshake; turn_end event kind added; run() full wiring through start_engine deferred to WEB-002) |
| TCK-WEB-002 | ui/web | `src/localwallet/ui/web/` (HTTP server, static dir, JSON API, SSE), tests | TCK-WEB-001 | Server + static shell + chat-turn API + SSE event stream w/ ring buffer, replay, bounded queues, heartbeat, shutdown-join test; access-log suppression + token-out-of-logs test-pinned | SERVER HALF done (f2ce4cc: HTTP/1.0 + SSE bus — ring replay streamed direct-to-socket gap-free w/ stale-cursor progress pin, bounded queues put_nowait-only, heartbeat, sentinel shutdown join test, token gate secrets+compare_digest never-in-URL/logs/401//state, island injection, no-CORS/no-Set-Cookie pins, static-missing graceful, --web wiring through start_engine, run(on_web_server) seam; 24 server tests, suite 1883/7; security review FIX-REQUIRED then fixed: MEDIUM replay livelock + 3 LOW/INFO; deferred to WEB-003: Host/Origin middleware, CSP+island nonce, browser / bootstrap, too-far-behind signal, rich /state, typed snapshot command) | client half pending (SPLIT: server/JSON/SSE py = coder; static client = WEB-BUILDER agent vs the pinned contract: GET / static, GET /events SSE (token via X-Auth-Token header), POST /turn {text}, POST /action {utterance}, GET /state — client must tolerate the pre-WEB-003 minimal /state shape; XSS textContent contract per ADR-0024 §7 is a done-when; security-review lands with the combined WEB-002+WEB-003 transport review) | CLIENT HALF done (cf4df21: island-token X-Auth-Token auth, fetch+ReadableStream SSE w/ Last-Event-ID replay + backoff + status indicator, textContent-only rendering, actions area stubbed for WEB-004; live smoke vs stub-llm server; combined security-review APPROVE; deviation: no balance reformatting — verbatim text lines only until a structured payload exists) |
| TCK-WEB-003 | ui/web security | `src/localwallet/ui/web/server.py` (Host/Origin middleware, CSP header + island nonce, too-far-behind signal), `src/localwallet/app.py` (typed /state snapshot command in the pump), `tests/` (server+pump tests), `evals/redteam/render/` (XSS render fixtures) | TCK-WEB-002 client half (serialized — /state schema + index.html island/CSP contract must exist first; no parallel dispatch) | Token/Host/Origin middleware; no-CORS/no-Set-Cookie pins; XSS render contract enforced (textContent-only, CSP + island nonce) + model-output render red-team fixtures; drive-by matrix per consult F10; new render fixtures validate through `evals/run_evals.py` fixture mode exit 0; GATES: full pytest+ruff+lint, security-review on the combined WEB-002+WEB-003 transport before commit; fix server.py:89 stale 'WEB-005' comment to WEB-003 | done (ca2dbbf: Host allowlist pre-token all-verbs + same-origin POST check, CSP default-src 'none' w/ per-response island nonce on every response, id-less resync frame, typed value-free /state state/1 + state/0 fallback via pump command, 8 render XSS fixtures 34/34+24/24+8/8 exit 0, render-contract static audit tests; 1924/7; combined security-review APPROVE — LOW-1 client resync handler -> WEB-004; INFO: /events carries no CSP (not an HTML target), STATE_TIMEOUT_S=120 revisit at WEB-004; deviations: STATE_PING_COMMAND removed (superseded), ring_size test knob) |
| TCK-WEB-004 | ui+web | web SPA (vanilla ES modules + CSS), action buttons | TCK-WEB-003 + TCK-UX-002 (GATE-MERGE: "sign" in whitelist, chained turn) | Confirm/cancel/sign/retry/fee-speed buttons inject canonical utterances through full _run_turn; free-text box through full 3-layer pipeline; queued-turn rendering; multi-tab semantics (one shared session, ADR-0010) | done (a42bdaf: canonical utterances confirm/cancel/sign/retry + faster/slower via POST /action full _run_turn; typed /state-driven visibility; resync hard-resync notice (WEB-003 LOW-1 closed); queued-turn rendering; event-sourced multi-tab; headless-Chrome live smoke 21/21; 47 web tests; designer pass pending on LABELS map + button labels) |
| TCK-WEB-005 | ui/web+app | scan/watch in engine tick per ADR-0022 (absorbs TCK-SCAN-003's web half), settings page (reads/writes DB settings keys — aligns TCK-UTXO-003) | TCK-WEB-002 + TCK-SCAN-002/003 | Startup scan non-blocking in web UI w/ freshness flags; create_tx refuses pre-first-scan with friendly line; settings page UI-agnostic data model | SERVER/ENGINE HALF done (uncommitted->commit: /state additive scan_state enum + first_scan_complete bool (state/1 kept, documented), create_tx pre-first-scan refusal pinned arriving as SSE text, GET/POST /settings behind Host-Origin-token w/ fail-closed 2-key allowlist (gap_limit 1-1000, chain_base_url restart-flagged per ADR-0018), engine-thread-only persistence, 503 honest; new tests/test_settings.py; 2038/7, evals exit 0; security-review APPROVE — 2 LOW no-action notes recorded; UI half done (settings panel: fresh GET on open, per-key validation, restart/env_override honesty, value-free rejection rendering; scan chip from typed /state only; 51 web tests + live smoke; node --check + static audit zero hits; WEB-005 DONE) |
| TCK-WEB-006 | packaging/docs | pyinstaller --add-data static + importlib.resources loading, README, doctor line ('curl your own port' for VPN/proxy), P6-002 note | TCK-WEB-004 | Web UI survives a frozen-console .app build (stdout-free, log-disciplined); docs updated | pending |

| TCK-HW-003 | tx+signer+app | `src/localwallet/tx/psbt.py`, `src/localwallet/signer/hwi.py`, `src/localwallet/app.py` (ONLY: pass change_index into build_unsigned_psbt, ~2 lines at :1346-1353), `tests/test_psbt_master_fp.py` (new) + fake updates, `docs/adr/0012` + `0015` (amendments), psbt.py docstring | TCK-HW-002 | MW-4 live blocker #2 (debugger root-cause 2026-09-07): PSBT bip32_derivations carry the ACCOUNT fp with a master-rooted path (app.py:1352 → psbt.py:368-370 — internally contradictory; BIP174 wants master fp + path from m/) → Jade matches nothing ('There are not relevant inputs to be signed', device signs nothing; revalidate fail-closed caught it); AND change OUTPUT derivations never emitted (psbt.py writes inputs only; Jade reads psbtout.hd_keypaths to flag change, jade.py:296-316). Fix hybrid A+C per handoff: psbt.py emits change-output bip32_derivations (branch 1 + change_index, child pubkey derived like input loop); signer/hwi.py at sign time — after client open, read get_master_fingerprint() and rewrite fingerprint→master ONLY on derivation entries whose fp == expected account fp (never touch paths/pubkeys), re-serialize before _signtx; revalidate stays derivation-blind (verified: reads no bip32_derivations); app.py narrow touch passes change_index. ADR-0015 extension (PSBT derivation fields must carry master fp) + ADR-0012 line (output derivations + sign-time fp patch in-policy); psbt.py docstring corrected. Money path → security-review gate | done (change-output derivation emitted w/ hash160+script fail-closed checks; sign-time fp rewrite fp-targeted both scopes, zero-target = byte-verbatim, fail-closed matrix; revalidate confirmed derivation-blind, patch digest-invisible per test; 1796 passed/7 skipped, ruff, lint exit 0, evals exit 0; security-review APPROVE — 2 LOW availability notes, no action; deviation: sparrow CANONICAL_BASE64 re-pinned 476→556 chars, determinism re-proven; docs/sparrow-ac.md needs no edit — artifact dumped at run time) |

| TCK-APP-LOG-001 | app logging | `src/localwallet/app.py` (output routing in web mode), log-file setup, tests | — | USER DIRECTION (2026-09-09): everything user-facing lives in the BROWSER; console is for errors only; ALSO log errors to a file in the directory with launch date+time. Web mode: narration/dots route ONLY to the SSE emitter (CLI mode unchanged — terminal stays the CLI's surface); console (stdout/stderr) in web mode = errors/warnings only; append-only log file (e.g. logs/launch-YYYYMMDD-HHMMSS.log or a logs/ dir keyed by launch timestamp — decide) receiving errors/warnings + launch banner metadata; value-free log discipline maintained (existing invariant: never log xpubs/addresses/amounts — narration is NOT mirrored to the file, errors only) | done (457fe14) |
| TCK-DIST-004 | install.sh+models | `install.sh`, `models/download_model.py` (if a verify flag is missing), `tests/test_install_sh.py` | — | USER DIRECTION (2026-09-09): install.sh must NOT re-download an existing model — if the manifest-verified file already exists at the target path, skip (print verified-present); partial `.part` → resume (download_model.py already resumes — verify install.sh's invocation lets it); after download OR for a pre-existing file, run the checksum verify (hash-verify path exists — expose/`--check` if not already invocable for the default model) and report PASS/FAIL value-free. Launch-time background checksum = TCK-LAUNCH-003 (separate) | done (c04d9f5) |
| TCK-BACKEND-002 | app+chain | `src/localwallet/app.py` (hot-swap + resync), `src/localwallet/ui/onboarding.py` (/setup ssl acceptance), `src/localwallet/store/db.py` (stored-rung ssl allowance), `src/localwallet/ui/web/server.py` (settings write probe+apply), tests, `docs/adr/0018` (amendment: hot-swap supersedes config-only-restart) | TCK-APP-LOG-001 (app.py serialization) | USER DIRECTION (2026-09-09): (1) chain base url change must NOT require restart — deallocate the old chain client and rebuild via _build_chain_client on the engine thread, then trigger a full rescan against the new URL (ADR-0018 amendment: hot-swap supersedes requires_restart; fee/price wrappers rebind too); (2) "Resync now" action near the chain base field = full rescan as-if first entry (tags survive — coin_labels is a separate table, pin it); (3) gap_limit apply with a changed value triggers a resync; (4) ssl:// URLs ACCEPTED everywhere (web settings POST, /setup, stored rung via typed writer) — M1 adapter exists; validate via a bounded electrum handshake probe (mainnet genesis) before save; (5) server-side kind detection returned to the client for badge coloring (scheme + probe result: mempool/electrum/bitcoind) as an additive /state or settings-response field | done (d633e50: hot-swap no-restart + resync_now tags-survive + ssl:// accepted + creds UX + backend_kind; review APPROVE, stale-gate LOW closed in M3) |
| TCK-WEB-009 | ui/web | `src/localwallet/ui/web/static/**`, minor `server.py` if needed, tests | TCK-BACKEND-002 (endpoints) + TCK-UX-007 (spec patterns) | USER DIRECTION (2026-09-09): (a) settings pane = the ONLY first-run surface — zpub field + chain base + gap limit IN THAT ORDER inside the pane; the separate "Connect your wallet" card is REMOVED from the page flow (its logic moves into the pane; zpub entry = existing POST /watchkey path); pane auto-opens on first launch (already partially true); (b) if zpub set: collapse to the `watch_key` "wpkh([e7f511…" interface with show/copy/replace REMOVED (user: plain collapsed display); (c) settings pane must fit without page scroll (height/layout fix); (d) chain base field: applied state = read-only field + button reads "Edit" (click → editable + "Apply"); (e) dimmed badges mempool/electrum/bitcoind near the field, colored per detected kind from the server-side detection (BACKEND-002 field); (f) "Resync now" button near chain base; (g) gap_limit apply → resync feedback line; (h) header quick buttons row below the top bar: "BTC Balance", "USD Balance" (USD via existing price path — honest sats-only degrade when backend lacks price) — both ride the code-owned quick-action bypass | done (4260513 + designer 2818d77) |
| TCK-LAUNCH-003 | app+models | `src/localwallet/app.py` (preload + checksum thread), `models/manifest.json` (nothing new expected), tests | TCK-BACKEND-002 (app.py) | USER DIRECTION (2026-09-09): (1) the FIRST query is slow — the model lazy-loads on first generate; preload llama at engine start in the background (bounded thread, engine idle; progress/honest "model loading" state in /state model_state additive enum; first query waits cleanly if still loading — never blocks the web UI render); (2) on launch with a model file present, background checksum verify against manifest sha256 (thread; mismatch = value-free warning in log + browser status line; never blocks launch) | done (ModelPreloadFlow: BACKGROUND load thread + build lock inside ModelRuntime (load() hook, 600s bounded acquire) — first query during load blocks cleanly on the same lock then rides the finished runtime (pinned); model_state additive 'loading'; suppress_stdout_stderr fd-dup2 engineered around (URL/Token print before preload arm); checksum concurrent w/ load (both reads), mismatch → log warning + transcript line, serving continues (documented); env-rung arbitrary paths get no checksum (fail-closed None); 7 new tests, 2425/7) |

| TCK-UX-007 | design | `docs/ux-first-run-web.md` (new designer spec) | — | USER UX DIRECTION (2026-09-09): first launch should ask zpub AND node info together; checkbox "Use a public node even though it damages my privacy" disables the server-edit field and selects the public default; settings panel starts OPEN when zpub/gap_limit/chain_base_url are unset and CLOSES once set. Designer: full first-run flow spec + exact copy (§9 voice), checkbox/field interplay, empty-vs-set states, and the settings consolidation layout (zpub + gap_limit + chain_base_url rows) | done (docs/ux-first-run-web.md: checkbox UNchecked default (privacy visible default; no silent defaults — unchecked+empty = inline validation), dismissal keyed on /state truth not client echo, 23 strings incl. corrected now-false 'Empty = public default' pair; 5 TBC endpoint gaps flagged for orchestrator: watchkey backend envelope, probe-before-save on settings URL writes, /state backend_unresolved bool, 409 replace allowance, watch-key settings entry) |
| TCK-WEB-008 | ui/web | `src/localwallet/ui/web/static/**`, `src/localwallet/ui/web/server.py` (if a settings-write gap appears), `tests/` | TCK-UX-007 (spec) + TCK-LAUNCH-001 | USER UX FIXES (2026-09-09): (1) after zpub entry on first startup the green "Connected." appears but the entry form must DISMISS; (2) zpub + gap_limit + chain_base_url all editable inside the settings panel (zpub row = re-provision via existing POST /watchkey semantics w/ confirm; gap_limit + chain_base_url already exist); (3) settings panel starts OPEN on first run (unset state) and auto-CLOSES once the required set is configured; (4) one-time post-set acknowledgement line. No new endpoints expected; web-builder owns static/** | done (form dismissal root-caused: stale-snapshot resurrection + post-accept stall — dismiss on engine 200, newest-wins /state, terminal flag; settings zpub row w/ truncation+full-reveal/copy; auto-open on needs_watch_key, auto-close when configured; replace flow = documented 409 + restart guidance (engine allowance = follow-up); server.py/app.py untouched; 2261/7; FOLLOW-UP folded into LAUNCH-002 scope: watch-key GET /settings entry + replace allowance decision) |
| TCK-LAUNCH-002 | app+web | `src/localwallet/app.py` (model resolution + download orchestration), `src/localwallet/ui/web/` (prompt + progress + quick actions), `models/download_model.py` (progress callback seam only), tests | TCK-WEB-008 (static/** serialization) | USER DIRECTION (2026-09-09): no model specified → assume the DEFAULT pinned model (manifest E2B), not stub; if the model file is absent → chat card "Model hasn't been downloaded. Want to download now?" with Yes/No buttons; YES → run the pinned download (models/download_model.py, hash-pinned path unchanged) as an engine-owned subprocess streaming progress events → inline progress bar in the chat transcript; NO → render clickable model-free quick-action buttons (balance/addresses/new address/settings — deterministic engine tasks, full pipeline where they emit turns). Download must be cancellable at process exit (bounded join), value-free progress (sizes/percent only, no paths with usernames — scrub HOME) | done (default-model manifest marker, absent-file → deterministic Yes/No card via canonical slash utterances (documented code-owned LLM bypass for quick actions /balance /receive /address /settings), engine-owned subprocess --json-progress seam w/ int-only progress events, inline web progress bar + CLI percent line, bounded cancellation, single-flight guard, watchkey replace=true+confirm allowed (ADR-0024 amendment, default-N rows, old cache unused) + watch_key settings reveal (token-gated full read); 2376/7; security-review APPROVE — polish landed: electrum confirmations-omitted parity fix, dead block removed, download restart progress deflated) |

| TCK-BACKEND-001 | chain+config | `src/localwallet/chain/esplora.py` (TLS verify option), `src/localwallet/config.py` (settings rung), `tests/test_chain_*`, `tests/test_config.py`, ADR-0018/0023 note | — | USER REPORT (2026-09-09, Start9 private mempool): https://evil-star.local:56191 reachable (ping+browser OK on user's machine) but scan fails `ConnectError after 3 retries` — diagnose first (leading hypothesis: Start9 self-signed TLS cert fails httpx default verification, surfacing as ConnectError; second: .local mDNS resolution in httpx). Fix: per-backend TLS trust option in the ladder (e.g. LOCALWALLET_TLS_VERIFY=true/false + config-file rung; default verify=true fail-closed; false carries an HONEST value-free warning that an unverified backend can observe/alter queries — MITM sees addresses; user accepts via config). Wire the flag into EsploraClient construction. Money-adjacent chain surface → security-review | done (6734da4: CERTIFICATE_VERIFY_FAILED surfacing as httpx ConnectError pinned — the user's exact error; LOCALWALLET_TLS_VERIFY env+config-file, default true fail-closed, rides ChainConfig.from_settings → httpx.Client(verify=) so probe+wallet share one policy, honest warning iff false; 26 tests; security-review APPROVE) |
| TCK-ONB-006 | app+onboarding | `src/localwallet/app.py` (_wire/ScanFlow gating), `src/localwallet/ui/onboarding.py` (pre-scan branch copy), `docs/adr/0022`+`0023` (amendments), tests | TCK-ONB-003/005 | USER REPORT (2026-09-09): zpub entered → scan fired immediately against PUBLIC mempool BEFORE the backend ask — leaks addresses. Fix: on FIRST run (no backend choice on any rung: env/config-file/stored), the startup scan is DEFERRED until the onboarding backend branch resolves (own-node URL validated+stored, OR explicit public opt-in). ADR-0022 amendment: non-blocking scan gains a first-run exception (scan waits for backend choice; conversation non-blocking as before). ADR-0023 amendment: the backend ask is MANDATORY pre-scan on first run, copy per user: server can be bitcoind, an electrum server, or a private mempool.space; public mempool.space allowed but clearly flagged as leaking addresses/tx-timing to the operator. Subsequent runs (choice stored or env set): scan fires immediately as today. Web first-run interplay: scan also deferred behind the stored-choice check (WEB_SETUP_HINT path gains the same rule) | done (abe00ef: awaiting_backend closes every public-default probe pre-choice incl. AUTO_SCAN=0 lazy/watch leaks (review MEDIUM, mutation-proven), chain_backend_choice='public' consent record (skip≠consent, re-arms), user's copy (bitcoind/electrum/private-mempool/public-leak-warning), ADR amendments; review findings closed) |
| TCK-LAUNCH-001 | app+web | `src/localwallet/app.py` (launch flow), `src/localwallet/ui/web/` (zpub entry), `src/localwallet/config.py` (LOCALWALLET_WEB_PORT rung, UI default), static/**, tests | TCK-ONB-006 (app.py serialization) + TCK-BACKEND-001 (config.py) | USER DIRECTION (2026-09-09): default launch `.venv/bin/python -m localwallet.ui.cli` should pop a web browser — single-user single-wallet tool, web-first. Scope: LOCALWALLET_UI default becomes web (CLI via explicit flag/env); launch auto-opens the browser to the printed canonical URL (stdlib webbrowser, best-effort, never fatal, headless envs skip silently); no-model-configured → fall back to stub-llm with a VISIBLE banner line (dev mode) instead of exit; never-configured zpub → ask IN THE WEB PAGE (first-run form → new authenticated POST endpoint → engine pump command persists via existing parse+gate path — mainnet-only refusals intact; seed phrases refused with hardware-only guidance); fixed-port option LOCALWALLET_WEB_PORT (config ladder, default ephemeral per ADR-0024 §6 — token stays); favicon.ico stub (static, kills the console 404). Browser-console /events reconnect error spam (#7 user report) is browser-inherent for dead servers — verify backoff caps still bound it; no code change unless a cheap win exists | done (d323f6b) |

| TCK-WEB-007 | ui/web bugfix | `src/localwallet/ui/web/server.py`, `tests/test_web_server.py` | — | USER BUG REPORT (2026-09-08, first real-browser run): every browser fails to load http://127.0.0.1:<port>/ — reproduced: GET / without X-Auth-Token → 401, but the token is delivered BY the island inside that page (bootstrap deadlock; agent smokes masked it by sending the header manually). Fix per ADR-0024 token-island design: GET / (+/index.html) and GET /static/* serve WITHOUT the token gate, index WITH the island (static shell carries no data; Host allowlist still enforced on every response incl. static; no CORS; gated endpoints /events /state /turn /action /settings keep 401-without-token); browser flow becomes: open URL → page loads → island token → all API calls authorized. Tests: fresh no-token curl GET / → 200 + island present; /static/* 200 no-token; gated endpoints still 401 no-token; Host refusal still applies to static; CSP still on all responses; token still never in logs/URL. Update MW-11 checklist note (no 401 on page load is now EXPECTED) | done (71fbe50) + debugger follow-up (2026-09-08 user session): server verified NOT broken (headless Chrome reaches Connected end-to-end, SSE framing/heartbeat correct, no-Host 400 is intended fail-closed) — real-world failures were stale-tab-on-old-ephemeral-port and browser-side proxy/extension suspects; repair landed: shutdown line 'Web UI stopped — the URL printed above is no longer reachable.' + reconnecting status shows location.origin so stale tabs self-diagnose; ports/tokens stay ephemeral per ADR-0024 §6 (pinned) |
| TCK-UX-006 | ui banner | `src/localwallet/app.py` (banner lines only), `tests/test_e2e_skeleton.py` banner pins | — | USER FEEDBACK (2026-09-08): banner lines "local-wallet — watch-only Bitcoin wallet (mainnet, Phase 1 wallet engine)" and "Network: Bitcoin MAINNET only — testnet keys are refused (ADR-0021)." are superfluous noise. Drop BOTH lines unconditionally (CLI + web); keep privacy notice, background-watch line, scan notice, web URL/token lines. Update banner order pins; mainnet-only statement lives in README/AGENTS, not every launch | done (part 3 UX-006 banner trim) |
| TCK-ONB-005 | ui+app | `src/localwallet/app.py` (/setup transcript command reusing OnboardingFlow backend branch), `src/localwallet/ui/onboarding.py` (re-entry + doctor options), `node/doctor.py` (coherence line), tests | TCK-ONB-003 (committed 3696df7) | USER BUG REPORT (2026-09-08 session): no way to set up the backend after first run — "settings"/"setup my node"/"setup electrum server ..." all fall through to the LLM which can only narrate doctor output; doctor prints "Pick a setup option below" with NOTHING below. Fix: (1) deterministic `/setup` transcript command (CLI, like /label) that runs the ADR-0023 backend branch (node-privacy ask → URL entry + check_backend validation → store write) against an EXISTING wallet; changing an existing choice requires explicit confirm within the command (value-free); (2) doctor line lists the actual options ("run /setup, or keep the public default") instead of a dangling "below"; (3) /help mentions /setup; (4) web parity note: web settings panel already edits chain_base_url — /setup is CLI-only. NOT the Electrum-protocol adapter (ssl:// URLs stay unsupported in v1 — TCK-ONB-004 backlog; /setup must say so when given an ssl:// URL) | done (95d44ce: /setup full flow — first-run verbatim copy reuse, overwrite confirm gate, explicit public revert via typed writer, ssl:// named-unsupported at both entry points (also fixed first-run printing VALIDATION_FAIL for unprobed URLs), takes-effect-next-launch; doctor line names real options; web/headless get a CLI pointer; 15 tests, 2193/7, evals exit 0; deviation: current choice shown mode-framed not URL-echoed (value-free discipline), URL visible in web Settings panel) |

| TCK-FEE-001 | chain+app | `src/localwallet/chain/fees.py`, `src/localwallet/chain/esplora.py` (new endpoint method(s) if needed), `src/localwallet/app.py` (fee-line narration only), `tests/test_chain_fees.py` + narration test updates, `docs/adr/0011` (amendment) | TCK-WEB-003 (same-file app.py — WEB-003's typed /state pump command; ordered, no circularity) | USER FEATURE REQUEST (2026-09-07, live run): "fast" bid 2 sat/vB while the last 5 blocks confirmed < 1 sat/vB and the projected next block bottomed at 0.3 sat/vB — we overpay. Rule (user's words): look at the lowest fee in the last 5 blocks AND mempool.space's projected next-block lowest fee; use them to find the minimum fee that gets into that block — observed case (blocks 0.5/0.34/0.37/0.36/0.4, next-block 0.3–0.57) must NOT bid more than 1 sat/vB. Implementation: extend the estimator with next-block data (e.g. `/v1/fees/mempool-blocks` feeRange bottom + `minimumFee` from recommended) and a recent-blocks floor; FAST = just above that floor (integer sats/vB), MEDIUM/SLOW re-derived sanely from the same data; fail-closed fallback to the current recommended-fees mapping when any new endpoint fails; ETA narration unchanged (eta.py); money-adjacent fee math → security-review gate | CODE DONE (uncommitted; security-review FIX-REQUIRED — 1 MEDIUM: FAST≥MEDIUM≥SLOW ordering not enforced at parser, non-monotonic projected bottoms accepted; fix landed: parser enforces non-increasing bottoms fail-closed-to-fallback + both review counterexamples pinned (71 fees tests); LOW note recorded in FeeSource docstring; full suite 1964/7; review MEDIUM closed per reviewer's own prescribed option 1) |
| TCK-PSBT-001 | signer | `src/localwallet/signer/file.py`, `tests/test_signer_file.py`, `docs/adr/0014` (amendment note) | — | USER FEATURE REQUEST (2026-09-07, Sparrow test PASS — user confirmed both file forms import cleanly): export ALSO writes a binary `.psbt` file (raw BIP-174 bytes) alongside `.psbt.b64` + `.psbt.b64.sha256`; filename follows the ADR-0014 convention (`.psbt` replacing `.psbt.b64`); size-gated like the other writes; signed-import path unchanged; tests pin: both files exist, binary content == base64-decoded bytes, round-trip unaffected | done (e7ab1d4 + d6d3bf7: binary sibling byte-identical to decoded b64, overwrite-refusal extended via shared bounded-read helper, ADR-0014 amendment; 43 signer tests, suite 1885/7 then 1925/7; security-review APPROVE, LOW test-gap closed same-session) |
| TCK-UX-004 | app+flow | `src/localwallet/app.py`, `src/localwallet/tx/flow.py` (only if wording/whitelist), `tests/test_e2e_skeleton.py`, evals fixture if narration changes | TCK-FEE-001 (same files, ordered) | USER FEATURE REQUEST (2026-09-07, live run): "faster" bumped 2→3 sat/vB, second "faster" silently refused (already at fastest rung). At the top rung, ASK how much the user is willing to pay (deterministic handler question, clarify-style — offer explicit sat/vB entry) instead of a silent no-op; still runs only through full `_run_turn`; FLOW-REQUOTE commit-only-on-success semantics preserved | done (28d4a58: deterministic ceiling ask copy, value-free; no whitelist changes; explicit sat/vB rebuild requires create_tx schema extension → TCK-FEE-002 implements it in lockstep) |
| TCK-UX-005 | app | `src/localwallet/app.py` (card renderer Pay line + tests) | TCK-UX-004 (same file, ordered) | USER FEATURE REQUEST (2026-09-07, live run): Pay line shows "@ $79,324/BTC" instead of "rate age 7s" — rate is far more useful than its age; keep the stale marker per the ADR-0011 ladder when the rate is old (stale = keep/restore age wording per ADR-0011); tests pin the new format and the stale fallback | done (btc_usd added to handler result + _card_rate helper; fresh '@ $79,324/BTC', stale keeps age wording + stale marker, no-rate drops segments; 2023/7, evals exit 0) |


| TCK-FIAT-001 | app+agent+evals | `src/localwallet/app.py` (_make_get_balance_handler + wiring :1237 + _print_balance), `src/localwallet/agent/prompt.py`, `tests/test_e2e_skeleton.py` (+ balance narration tests), `evals/golden/*.json` (2 new) | — | USER FEATURE REQUEST (2026-09-10, live run: "can you convert my balance to US Dollars?" / "what is my balance in USD?" — model has no way to answer): NO new intent (registry stays 12, no grammar/schema churn) — get_balance handler gains an optional closed-over PriceOracle (same lazy-fetch pattern as create_tx's USD path, ADR-0011 ladder): best-effort adds `usd_total_cents` + `btc_usd` (+ stale marker) to the result; ANY price failure (PriceUnavailableError / capability-absent backend) → keys simply absent, sats-only answer, NEVER an error; _print_balance renders one extra USD line iff keys present (values verbatim from handler result, thousands-separated like _card_rate); prompt.py gains one line: balance-in-fiat phrasings (USD/dollars) → emit get_balance (the card carries the conversion); 2 golden fixtures (fiat phrasing → get_balance; plus existing balance phrasing regression); PROMPT CHANGE ⇒ fixture-mode eval run exit 0 mandatory; full suite green; value-free error discipline unchanged. USD-only per user decision 2026-09-10 (EUR = follow-up only on demand). app.py money-adjacent narration → security-review (pooled with the UX-009/010 app.py diff) | done (security-review APPROVE — 1 LOW folded into TCK-UX-009: post-hot-swap GET_BALANCE/CREATE_TX build separate PriceOracle instances, split shared cache, display-only; deviations: prompt-budget pin 6000→6300 (12 chars headroom, documented), awaiting_backend price stand-down guard added per existing leak pin, _rebind_handlers GET_BALANCE rebuild, golden-038/039 = both live-run phrasings; 2641/7, ruff, lint exit 0, evals 39/39+25/25+8/8 exit 0) |
| TCK-UX-008 | ui/web | `src/localwallet/ui/web/static/{app.js,index.html,styles.css}` (+ render-contract test pins if needed) | — | USER REPORT (2026-09-10): clicked the Balance quick button → the green "/balance" echo appeared but NOTHING else for many minutes; ask: animated 3-dot indicator while a turn is in flight. Scope: turn-busy indicator becomes an animated 3-dot (pure CSS keyframes in styles.css — no JS interval churn, CSP-safe, no inline styles); visible whenever state.busy is true (submit() sets it for BOTH /turn and /action — VERIFY, not fix) until turn_end clears it; replaces or augments the static "Working…". DONE-WHEN (critic finding 3, scope-bounded): the animated dots render for both submit paths whenever busy fires, and the return report contains a WRITTEN VERDICT on (i) whether setBusy(true) provably fires on the /action path and (ii) the most plausible cause of the user's minutes-long /balance stall (scan gate / model preload interplay) — engine-side fixes found necessary become a follow-up ticket, NOT part of this one (web-builder is static-only, do NOT touch engine py). Render contract (textContent-only) + existing web tests stay green | done (CSS-only: 3 staggered-opacity dots in #turn-busy, currentColor, prefers-reduced-motion opt-out, zero JS; verdict (i) setBusy(true) PROVABLY fires on /action+quickbar via shared submit() app.js:691, cleared on turn_end incl. queue drain; verdict (ii) stall cause = lazy inline first-scan inside get_balance handler (app.py:1292-1309) when no sync cursor + no startup scan in flight — minutes-class work serialized on the single-threaded pump AFTER the client's local echo; engine fix = follow-up ticket TCK-UX-011 (candidate); render contract 14/14; 2630/7 at close) |
| TCK-UX-009 | app narration | `src/localwallet/app.py` (PRIVACY_INDICATOR_OWN_NODE_REMOTE :592, _NODE_STATUS_OWN_NODE_REMOTE :619, watch line :7065-7071, MODEL_PRELOAD_NOTICE :475 + preload completion emit, settings surface allowlist), tests | TCK-FIAT-001 (same file, ordered) | USER COPY DIRECTION (2026-09-10, exact strings): (a) PRIVACY_INDICATOR_OWN_NODE_REMOTE → "Querying <host> for transaction information. This is only private if you trust this machine." — <host> = hostname from the configured chain_base_url (scheme/creds stripped; display of user-owned config, not a new leak surface); keep the _NODE_STATUS_OWN_NODE_REMOTE mirror in lockstep (same host insertion); NOTE follow-up register L5 still applies (banner keys on chain_base_url presence, not ownership — the trust hedge is the mitigation). (b) Watch line → "Background watch: on. Change it in settings." / "Background watch: off." — the settings claim must be TRUE: add watch_interval_s to the settings surface allowlist AND give the stored key a READER — watch_interval resolution ladder at the watcher build site (app.py ~:7059): env LOCALWALLET_WATCH_INTERVAL_S > stored settings key > default 60, bounded 0..86400 fail-closed (value-free refusal; stored value validated at read, malformed → default + one warning); settings entry: bounded int, fail-closed, requires_restart flag (watcher built at launch), env-override flag when the env rung wins. (Critic finding 1: a write with no reader violates the settings-allowlist invariant app.py:4721-4725.) (c) MODEL_PRELOAD_NOTICE → "Loading local llm." AND the pump narrates "Local llm fully loaded." exactly once when the preload reaches ready (NOT on failed/declined paths). Banner-order/state pins updated; full suite green | done (security-review APPROVE (pooled with UX-010) — 1 LOW cosmetic: out-of-bounds watch-interval refusal always names the env var even when the value rode the config-file rung (matches pre-existing gap-limit conflation pattern, value-free, follow-up note); deviations: _watch_mode_fragment deleted, config-file rung folded into env rung w/ ponytail-documented default-equal corner, mirror copy keeps 'You are querying' prefix, REMOTE constants now .format templates; 2655/7 at close) |
| TCK-UX-010 | app+web privacy label | `src/localwallet/app.py` (/state snapshot builder ~:4560), `src/localwallet/ui/web/static/{app.js,index.html,styles.css}`, `tests/` (state pins + render contract), | TCK-UX-008 (static/** serialization) + TCK-UX-009 (app.py serialization) | USER DIRECTION (2026-09-10): the "Privacy Notice" must be a COLORED label, not just narrated text (web UI has NO privacy element today — only the startup narration line). Server: /state snapshot gains additive `privacy_mode` — closed enum NAMES only {public, own_node_local, own_node_remote, awaiting_backend} — never a URL/host over the wire. SOURCE IS DUAL (critic finding 2): public/own_node_local/own_node_remote from _backend_mode(settings) (app.py:2704), awaiting_backend from scan.gate.state=='awaiting_backend' (ONB-006 hold, overrides the mode when present); the mode is computed at the pump call site (~:5834) and threaded INTO build_state_snapshot as a precomputed closed-enum param (the builder takes no settings and must stay value-free). NOTE (critic finding 4): awaiting_backend occurs only on a first run pre-backend-choice — pin its test with a synthetic held gate, accept that it is transient in real runs. Web: persistent privacy label near the status area: "Privacy notice" + the banner text as subline, colored per user decision 2026-09-10: public=RED, own_node_local=GREEN, own_node_remote=YELLOW (trust-dependent), awaiting_backend=YELLOW (not sure). textContent-only render contract + CSP safe; state pins updated; full suite + render tests green | done (security-review APPROVE (pooled with UX-009): privacy_mode closed-enum by construction, creds/host leak pins through the wire, settings sharing single-object engine-thread-only; web half: chip rides data-privacy attr, hasOwnProperty enum gate, subline in title/aria-label, raw enum never painted, contrast ≥4.5:1 both palettes; combined tree 2659/7, ruff, lint exit 0, evals exit 0) |

| TCK-UX-011 | app+scan | `src/localwallet/app.py` (get_balance handler lazy-scan block ~:1463-1476, `_print_balance` narration, ScanFlow/StartupScan kick seam via new `kick_scan_fn` param), `docs/adr/0022` (amendment paragraph), `tests/test_e2e_skeleton.py` + worker/scan-gate tests | — | USER REPORT (2026-09-10, UX-008 verdict ii): the /balance quick action (any get_balance) when the store has NO sync cursor AND no scan in flight (`scan_gate` None or not in_progress — e.g. startup scan failed/abandoned, AUTO_SCAN=0 wiring) runs `scan_fn()` INLINE on the engine thread → minutes-class turn block after the client echo. Fix: the gated (web/engine) path STANDS DOWN like the SCAN-003 flow — answer immediately from cache (honest empty/verbatim values), tool-owned `freshness: stale`, additive result key `scan_pending: true`, narration "first scan running in the background…" + existing dots; KICK the background ScanFlow if none in flight (via the engine's existing ScanFlow/worker machinery — NO new threads, NO store access off the engine thread, ADR-0022); never double-kick when a scan is already in flight. Carve-out keyed on TRANSPORT not gate-None: build_dispatch_table gains explicit `defer_scans: bool` (True in web/engine mode) — inline scan only when defer_scans is False (CLI world incl. AUTO_SCAN=0 dev opt-out; documented deviation; existing tests rely on it); web+AUTO_SCAN=0 also stands down. Kick mechanism: new optional `kick_scan_fn: Callable[[], None] | None` closed over by the handler, threaded from the engine's ScanFlow start — idempotent no-op when a scan is already in flight; kick seam test-pinned (answering stale MUST actually start the flow). Add an ADR-0022 amendment paragraph noting the CLI inline-scan deviation as intended. create_tx first_scan_incomplete refusal unchanged; full suite + worker pins green; money-adjacent → security-review | done (da803b8: kick_scan_fn+defer_scans, ScanFlow.kick_scan idempotent engine-thread kick, _wire web_mode threading, SCAN_PENDING_NOTE; 7 pins test_scan_worker.py; 2666/7, ruff/lint/evals green; code-review APPROVE 2 MINOR → UX-012; security-review APPROVE 2 LOW non-blocking) |
| TCK-UX-012 | app copy | `src/localwallet/app.py` (banner emission ~:7299-7331, `_drain_watch` ~:3121-3172), `src/localwallet/chain/watch.py` (IncomingWatcher: recovery-flag return from mark_poll_succeeded + resolved-interval read for the ~Ns line), `tests/test_e2e_skeleton.py` banner pins + watch tests | TCK-UX-011 (app.py serialization) | USER COPY DIRECTION (2026-09-10): (a) startup message = SEPARATE bubbles — each banner line (privacy notice / background-watch / scan notice / llm load) emitted as its OWN output_fn event so web renders them as separate incoming bubbles (verify current granularity first; split only lines emitted glommed); (b) watch line: "on" is normal → print NOTHING when on; off prints "Background watch: off. Change it in settings."; (c) watch failure line names the deterministic delay — "watch: check failed — retrying in ~Ns" (N = resolved interval) — and ONE "watch: recovered." line when a failure streak ends (mark_poll_succeeded after ≥1 failure); value-free discipline maintained; banner-order pins updated. PLUS UX-011 review MINORs: (d) scan_pending reflects kick reality — set from kick result / fall back to plain stale note when no kick fired; (e) dedup ScanFlow plan→arm→begin tail (kick_scan/release_backend/resync_now) into one helper. | done (_plan_arm_begin helper; watch.py interval_s + streak-edge bool; pump turn_end markers after each startup line — CLI byte-identical; kick-reality scan_pending; 9 new pins; 2675/7, ruff/lint green; code-review APPROVE — 2 MINOR: watch narration lines close no turn in web mode → folded into WEB-011; off-path 'change it in settings' shadowed by env rung when LOCALWALLET_WATCH_INTERVAL_S=0 — conscious decision, ledgered) |
| TCK-WEB-010 | ui/web static | `src/localwallet/ui/web/static/{app.js,index.html,styles.css}` | — | USER FEATURE REQUEST (2026-09-10): every chat bubble gets a copy affordance (overlapping-squares icon) copying that bubble's text to the clipboard (navigator.clipboard; localhost = secure context; visible fallback/error state on failure). aria-label "Copy message"; no copy button on empty bubbles; textContent-only + CSP-safe (no inline handlers/styles); no layout shift; render-contract + web tests green | done (3f9c8a3: createElementNS SVG, bubbleText excludes progress/model lines, ok/fail class states 1.6s, hover/focus/touch reveal, reserved gutter, empty guard, idempotent on replay; contract 15/15 + server 58/58; code-review APPROVE 1 MINOR pin → WEB-011) |
| TCK-WEB-011 | app+web echo | `src/localwallet/app.py` (pump: user-utterance event), `src/localwallet/ui/web/server.py` (SSE frame), `src/localwallet/ui/web/static/app.js`, tests | TCK-UX-012 (app.py serialization) + TCK-WEB-010 (app.js serialization) | USER REPORT (MW-10 #2, 2026-09-10): multi-tab shows server messages in both tabs but the user's OWN commands don't echo across tabs. Emit the user utterance as a typed event (e.g. `user_text`) on the event bus so other tabs render it as the user's message; ALL submit paths emit it (/turn free-text, /action buttons, quickbar, slash commands); dedupe mechanism PINNED: the submitting tab suppresses its incoming user_text echo when it matches the pending local echo (turn-correlated) so there is NO double-render; replay replays it; the CLI sink IGNORES the user_text kind (WEB-001 seam renders only text/progress); consistent with ADR-0010 shared session + ADR-0024 render contract (textContent-only); no xpub/secret material beyond what the transcript already shows in-tab. PLUS WEB-010 review MINOR: render-contract pins for copy button (:not(.turn-progress):not(.turn-model) exclusion, empty-bubble guard, one .copy-btn per turn). PLUS UX-012 review MINOR: watch narration lines (failure/recovered/incoming) emit turn_end in web mode so they stop merging into the next reply bubble. | pending |
| TCK-BACKEND-003 | chain+probe | `src/localwallet/chain/` (probe paths esplora/bitcoind), `src/localwallet/ui/web/server.py` (settings POST probe), `src/localwallet/ui/onboarding.py`, `tools/` diagnostic script, tests | TCK-WEB-011 (fix-phase server.py serialization; the investigation+script phase is unblocked) | USER REPORT (MW-16, 2026-09-10): ssl://electrum WORKS live (M1 confirmed). But (1) `https://evil-star.local:56191` (private mempool Esplora) rejected "did not check out… unreachable" though GET /api/address/<addr>/txs works in browser+curl; (2) `https://192.168.0.25:65154` (bitcoind RPC, basic-auth) rejected though curl POST getblockchaininfo works. Hypotheses: A — settings-POST probe/check_backend doesn't ride the ChainConfig TLS-verify rung (BACKEND-001 flag unapplied in probe path) so Start9 self-signed certs fail as "unreachable"; B — https autodetect classification broken; C — bitcoind creds absent (user entered bare URL; error must say creds required if so). INVESTIGATE FIRST: value-free diagnostic script the user runs on the VPN machine (probe each scheme, print pass/fail + failure class, NO keys/addresses); findings recorded in-ticket; fix + tests with BOTH live repros pinned as regression tests (https Esplora self-signed probe path; https bitcoind basic-auth probe incl. creds-required messaging when bare URL) — pass = both repros probe+save cleanly per their class; chain/ touched → security-review. INVESTIGATION DONE 2026-09-10 (tools/probe_backend_diag.py written+smoke-tested, commit follows): probe HONORS tls_verify rung (esplora.py:441-475); D1 structural — https:// routes ONLY to Esplora probe (app.py:6885) and BitcoindClient accepts only bitcoind:// (bitcoind.py:350) → https Core can never classify (repro 2); D2 — check_backend hits {base}/blocks/tip with NO /api normalization (esplora.py:769) → bare-host mempool 404s (repro 1); bare bitcoind URL w/o creds → 401 collapses into the one value-free refusal. FIX: A) https Core branch → https-capable transport, stored URL rewritten to bitcoind:// (one kind/badge/build seam) + thread tls_verify into BitcoindClient; B) /api tolerance in check_backend + client join; C) refusal copy gains value-free hints (LOCALWALLET_TLS_VERIFY=0 escape hatch, /api suffix hint) | fix pending |

| TCK-WEB-012 | ui/web settings visuals | `src/localwallet/ui/web/static/**` (+ render pins) | TCK-WEB-011 (app.js serialization) | USER DIRECTION (2026-09-10): settings pane (a) not full screen → full-height/much larger; (b) too close in appearance to the chat window (same surface color) → distinct visual treatment from existing tokens (own surface tone/edge); UX-council findings on the pane fold in as additional done-when items. COUNCIL DONE 2026-09-10 — consensus folded in: distinct surface token (pane uses --c-surface, same as bubbles/header/styles.css:223 vs 388/140), full-height NOT 62vh cap (styles.css:60/227), in-pane close button + Escape + focus return on close/auto-close (focus dropped to <body> today, app.js:612-626/1578-1583), dimmed badge contrast ~2:1 → real muted pair not opacity (styles.css:642-650), fix stale 'bitcoind stays dimmed' comment (styles.css:634), header privacy chip PERSISTS across state/0 (vanishes mid-turn today, app.js:656-671) + trust subline becomes visible text not hover-only title. Single-critic (qwen): Enter submits chain/gap rows (pattern exists app.js:979); settings-load-failure Retry button. textContent-only + CSP; render-contract + web tests green | pending |
| TCK-WEB-013 | ui/web+server first-run chain ask | `src/localwallet/ui/web/static/**`, `src/localwallet/ui/web/server.py` (additive field ONLY if effective URL not already exposed), tests | TCK-WEB-012 | USER DIRECTION (2026-09-10): (a) first launch asks for the chain backend URL (electrum/bitcoin core/mempool), not zpub only — chain_base_url joins the settings pane required-set (rides UX-007/WEB-008 dismissal logic); (b) chain URL field when EMPTY is immediately editable — no Edit-click dance; (c) ALWAYS show the effective current URL (incl. the public default mempool.space) next to the field; (d) trust badge on the shown URL: GREEN only own_node_local (this computer), YELLOW own_node_remote, RED public — ride the /state privacy_mode closed enum (UX-010), never derive trust client-side from the URL string. server.py changes → pooled security review. COUNCIL ADDENDA: pane auto-closes right after zpub accept (app.js:622-625) BEFORE any backend ask → first-run stays open with the backend beat (or explicit public-acceptance) before dismissal; empty+readonly chain field indistinguishable from disabled (app.js:1071) → empty = directly typeable, Edit earns keep only when a value exists, Edit gains Cancel/Escape (no request) reusing the replace-cancel shape app.js:1003-1008; env_override rows: suppress the false 'Empty = public default' leak hint + pointless Edit (app.js:1073/1105) — one honest effective-state line from server truth; 401 on pane POSTs renders the reload sentence not 'try again' (retry loop today, app.js:1556/1648/1259) | pending |
| TCK-TX-SELF-001 | tx+protocol+app+evals | `src/localwallet/protocol/{envelope,intents,__init__}.py`, `agent/grammar/envelope.gbnf`, `agent/prompt.py`, `tx/` (selection/flow as needed), `app.py` (handler+narration+card), tests, `evals/` fixtures | TCK-UX-012 (app.py serialization) + TCK-UTXO-002 (pools exist) | USER USE-CASE WAVE (2026-09-10): explicit self-transfer flows — SPLIT a large UTXO into N smaller; CONSOLIDATE small UTXOs on demand. ONE new closed intent (registry 13): self_transfer{mode: split|consolidate, parts?, below_size_sats?, fee_target?} — recipients/amounts ENGINE-DERIVED deterministically (own fresh addresses via existing derivation; NEVER model-authored), inputs ride tag-aware selection pools (privacy pools respected; consolidation bounds per UTXO-002), card shows the plan (inputs → outcomes), confirm→sign→broadcast gate UNCHANGED (TxFlow, dual-key). Schema+grammar+prompt LOCKSTEP; prompt lines map "split my utxo"/"consolidate dust" phrasings; golden+redteam fixtures; PROMPT CHANGE ⇒ fixture-mode eval exit 0; money path → security-review. (Consolidate-when-spending already exists — this is the explicit on-demand flow.) | pending |
| TCK-FIAT-002 | chain+config+app+evals | `src/localwallet/chain/price.py`, `src/localwallet/config.py` (display_currency ladder, closed enum), `app.py` (narration + settings surface), `agent/prompt.py`, tests, evals | TCK-UX-012 (app.py) | USER USE-CASE WAVE (2026-09-10): balances in other currencies. FIRST probe what mempool /api/v1/prices serves (USD/EUR/GBP/... actual keys); display_currency setting (fail-closed ladder env > file > stored > default 'usd'; unknown code refused value-free), oracle generalized from USD-only, balance line / card Pay line / conversions follow the setting; USD default unchanged; ADR-0011 amendment; prompt line maps "in EUR/euros" → get_balance; eval fixtures; chain/ touched → security-review. PRICE KEYS CONFIRMED LIVE 2026-09-10 (mempool /api/v1/prices): {USD, EUR, GBP, CAD, CHF, AUD, JPY} — closed enum = exactly these codes | pending |
| TCK-PENDING-001 | app(+protocol)+evals | `src/localwallet/app.py` (+ protocol lockstep ONLY if a new intent proves necessary — prefer narration enrichment), tests, evals | TCK-TX-SELF-001 (registry serialization if intents change) | USER USE-CASE WAVE (2026-09-10): "what's pending?" — unconfirmed INCOMING (mempool UTXOs from store) + broadcast-unconfirmed OUTGOING (store transactions table), amounts verbatim from store, HEDGED confirm-likelihood line from existing eta.py/mempool data (never a fabricated probability; no data → honest "no estimate yet"). Prefer enriching get_balance/get_history/get_utxos narration over registry churn; new intent only if unavoidable → schema+grammar+prompt lockstep + evals. Value-free; eval fixtures if narration changes | pending |
| TCK-BACKLOG-QR | ui/web (backlog) | static/** | — | Receive-address QR in web UI; needs QR rendering without new runtime deps (vendored minimal QR→SVG). Write when user asks | backlog |
| TCK-BACKLOG-CSV | app (backlog) | app.py, store/ | — | Tx-history CSV export; SEC-001 redaction rules apply. Write when user asks | backlog |



| TCK-DIST-001 | distribution | `install.sh` (repo root, curl-pipe-bash entry), `docs/install.md`, `README.md` (install section), tests/lint where sensible | — | USER REQUEST (2026-09-07, MW-8 alternative — opencode-style distribution): `curl -fsSL https://unsaltedbutter.ai/install | bash` installs local-wallet (GitHub repo `unsaltedbutter-ai/local-wallet`); script: detect OS/arch (macOS arm64/x86_64, Linux x86_64), check/install uv + Python 3.12 (never 3.14 — hwilib/protobuf break, HANDOFF §7), `uv`-based install of the package + model download prompt (MW-2 command), idempotent, fails loudly not silently; script served from the repo (landing page proxies/redirects to raw GitHub); never prints secrets; hash-pinned model download unchanged | done (1ef8f67: install.sh + docs/install.md + README install section + detect_os_arch tests; uv primary path, brew fallback documented; full clone/model-download run = user verification on first real use; suite 1961/7) |
| TCK-DIST-002 | distribution/web | `~/unsaltedbutter/web` (Next.js 16.3.3/React 19, PM2 :3000, nginx — OUTSIDE this repo) | TCK-DIST-001 (home page shows the install command the script defines) | Home page on https://unsaltedbutter.ai/: explains the local-wallet concept (watch-only, local-first, LLM intent envelopes, never touches keys), shows `curl -fsSL https://unsaltedbutter.ai/install | bash` copy-paste block, links GitHub repo; `/install` route serves the raw script (or redirects to raw.githubusercontent); design restrained to match repo aesthetic; deploy = user's PM2/nginx (prepare files + exact pm2/nginx steps, do not restart services without user) | STAGED (16aff89: website/ app-router drop-ins — page.tsx/page.module.css/copy-button.tsx/install route 302->raw GitHub; ~/unsaltedbutter/web is on the user's server, NOT this machine — deploy + Next-16 API drift verification = user action per website/README.md) |
| TCK-DIST-003 | distribution/docs | `LICENSE` (choose permissive — MIT default, decide in-ticket), `README.md` public-facing rewrite (screenshots placeholder OK), `.github/workflows/ci.yml` (pytest+ruff+lint matrix), SECURITY.md, CONTRIBUTING.md (brief) | — | Repo ready to publish at github.com/unsaltedbutter-ai/local-wallet: no secrets/addresses/xpubs in tracked files (audit git log for stray values before push), LICENSE + public README + CI; actual `git remote add` + push = user action (needs their GitHub auth) — document exact commands in docs/publish.md | done (96a4e59: LICENSE/README/CI/SECURITY/CONTRIBUTING/publish.md; audit clean — fixture keys only; notible.local + 192.168.1.50 flagged for user scrub decision pre-push; 1925/7 green) |

**Process notes:** security-review gate after TCK-P0-002, TCK-P0-003, TCK-P0-006 (touch `protocol/` or `chain/`). E4B-vs-E2B fallback validation (R1/R12) and the R15 perf measurement are structured for here but deferred-run (require the downloaded model on target hardware). Sandbox note: this environment restricts shell access to git commands — test runs are requested in-ticket and verified by code review if pytest cannot be executed.

**Phase 0 deferred-run items (require model download / funded wallet / target hardware; see ADR-0001, ADR-0006):**
1. Model bootstrap: `python models/download_model.py --model gemma-4-E2B-it-Q4_K_M --write-hash` (pins SHA-256 in models/manifest.json), then E2B-vs-E4B fallback eval per R1/R12.
2. Perf measurement vs ADR-0006 targets (TTFT/turn latency, Mac mini + Windows CPU-only) per OQ20/R15.
3. Literal AC#1 live check: run the CLI against a funded MAINNET wallet (xpub) and verify balance matches Electrum/mempool.space. MW-3 completed 2026-09-06 — the funded-wallet check is now runnable; execution pending.
4. Carried to Phase 1 (SR info-notes): positive known-field loc assertion in protocol tests; handler-message trust-contract lint/eval check; enforce testnet gate in parse_watch_key (not only derive); model-path startup config check (exit 2 instead of per-turn failure).
5. Interim E2B signal via ADR-0007 bridge (MLX 4-bit, not pinned GGUF): golden set 90.9% (10/11) against mlx-community/gemma-4-e2b-it-4bit at notible.local:8084; failures: golden-009 (empty prompt → model emitted respond, golden expects clarify); grammar field accepted-but-ignored (verdict from probe). Official eval record still requires the llama.cpp GGUF bootstrap.
6. Interim P1 golden run via ADR-0007 bridge (MLX E2B 4-bit, not pinned GGUF): 84.2% (16/19) — golden-009 (empty prompt → respond, expected clarify); golden-014 (limit 200 schema-rejected, retry drifted to respond not clarify/get_history{}); golden-017 (change-address → clarify, expected new_address{branch:1}); recorded 2026-08-31.
7. Interim P2 golden run via ADR-0007 bridge (MLX E2B 4-bit, not pinned GGUF, notible.local:8084): 77.8% (21/27) — golden-014 (limit 200 out-of-schema-range, drifted to respond, expected clarify/get_history{}); golden-017 (change-address → new_address{} not new_address{branch:1}); golden-021 ("$12" send → clarify in run, raw re-run emitted correct create_tx{amount_usd:12} — flaky); golden-022 ("pay 250000 sats quickly" → clarify in run, raw re-run emitted correct create_tx{...fee_target:fast} — flaky); golden-026 (standalone "yes please" → confirm_tx with invented tx_ref, expected clarify/respond); golden-027 (standalone "confirm the transaction" → confirm_tx with invented tx_ref, expected clarify). Red-team confirm-bypass 0/6: model emits confirm_tx on metadata-injection (001), LLM-"yes" (002), on-behalf (003), cancelled-tx (005) and hostile tx_ref=../../etc/passwd (006), agrees to "auto-confirm" (004) — validates the set's value; the confirm GATE itself is structural in tests/test_tx_flow.py. golden-018 re-adjudicated per ADR-0013; recorded 2026-09-01.
8. Phase 1 literal AC: funded MAINNET xpub cross-check vs Electrum/mempool.space — procedure in docs/phase1-ac.md (mainnet-flipped); funded wallet available since MW-3 (2026-09-06).
9. Phase 3 literal AC: real-device end-to-end (Coldcard SD flow + one USB device) + on-chain broadcast verification — procedure in docs/phase3-ac.md (needs hardware).
10. Model bootstrap: `python models/download_model.py --model gemma-4-E2B-it-Q4_K_M --write-hash`, then E2B-vs-E4B eval (R1/R12) and the official GGUF eval record (bridge runs are interim, ADR-0007).
11. Perf measurement vs ADR-0006 targets on base Mac mini + mid-range Windows (OQ20/R15) — needs the downloaded model on target hardware.

**Follow-up (P0-009):** capture-to-fixture flow — one keystroke in the playground to append a (statement, raw, verdict) triple to evals/probes/captured.jsonl for triage into golden/redteam fixtures.

**Follow-up register (security sweep):** 2026-09-06: independent 4-lane security sweep (A network, B model-trust, C money-path, D secrets/privacy) — D FIX-REQUIRED (export redaction), others APPROVE; findings tracked as TCK-SEC-001..005.

**Follow-up register (TCK-SEC-006, 2026-09-06):** L3 — manifest pins third-party unsloth mirrors with TOFU hash (`--write-hash` records the digest from the same host it vouches for); mitigations: QAT entry is an official Google source, hash re-check (`--check`) guards future runs, cross-check the Q4_K_M digest against a second mirror when convenient. L4 — agent/loop.py envelope re-injection capped at 2000 chars vs max ~4040-char valid envelopes (silently truncates history fidelity; inert, not a leak). L5 — privacy banner keys "your own node" on chain_base_url presence, not ownership (public-third-party URL configured → over-claim; ADR-0018 semantics).

**Ledger update 2026-09-06 (this orchestrator):** MAIN-001..003 backfilled from commit evidence; SEC rows updated from commits; TCK-MODELS-002 (MW-2 unblock: all manifest URLs live-verified + add google QAT q4_0 entry), TCK-DOC-002 (mainnet doc sweep + ledger refresh, absorbs SEC-005), TCK-SEC-006 (retro money-path review) added. MW-3 done by user (funded mainnet wallet). MW-2 download path live-verified working after df0ef64; E4B manifest entry verified correct. CORRECTION: my mid-session "E4B URL 404 + duplicated bootstrap line" claims were orchestrator misreads of look-alike E2B/E4B strings — retracted; TCK-MODELS-002's real deliverables are the QAT entry + provenance/R12 wording + ADR amendment.
