# HANDOFF.md — local-wallet orchestration handoff

- **Written:** 2026-09-04, end of autonomous run (Phases 0–5 complete) — **amended 2026-09-06:** the security sweep (TCK-SEC-001..004) and the mainnet flip (TCK-MAIN-001..003, ADR-0021) landed since this was written.
- **Branch:** `dev/plan-run-1` · **HEAD:** `8a84c23` (session 2026-09-15 part 10 — see §0c7; TASKS.md is authoritative) · Working tree clean except the user's untracked ci-failure-20260915.md triage note.
- **Source docs:** PROJECT.md (spec), TASKS.md (ticket table — authoritative for ids/status), docs/adr/0001–0015 (decisions), docs/phase1-ac.md / phase3-ac.md / sparrow-ac.md (deferred-run procedures), docs/device-notes.md
- **Suite:** `pytest tests/ -q` → **4518 passed / 5 skipped** (session 2026-09-15 part 10; evals 78/78 + 28/28 + 8/8 exit 0) · ruff clean · `python tools/lint_network.py` exit 0 · model mode on pinned GGUF: **26/53 = 49.1%, exit 1 — the ENFORCED gate failing is the recorded, accepted outcome** (misses are model-quality limits; redteam "misses" are the by-design structural-gate case)

## 0c7. SESSION UPDATE 2026-09-15, part 10 (read this first; parts 1–8 are history)

- **NINE tickets landed + 3 debugger root-causes + the skipped-test sweep, all committed and reviewed (code-review every slice, security-review every money/chain/protocol path). Baseline now 4518 passed / 5 skipped; evals 78+28+8 exit 0; ruff/lint clean; HEAD 8a84c23.**
- **Landed (commit order):** TCK-CANCEL-001 (aaa75c9 — debugger: the deterministic cancel WORKED but `_run_turn` invoked the model anyway → hallucinated "I cannot cancel" + create_tx resurrection from CANCELLED; fix = early return before `loop.run`, hostile-model + zero-call pins) → TCK-DIAG-006 (b99e040 — debugger: bitcoind scan did ONE FULL scantxoutset WALK PER PROBED ADDRESS (41 walks ≈ the user's 2-min-per-dot); fix = `prefetch_scan` one-walk-per-window capability seam, persist-equality pinned) → TCK-ELECTRUM-001 (e340a6f — LIVE-TEST finding: electrum.blockstream.info rejects verbose transaction.get −32603 → whole scan failed on the WEB-022 suggested server; fix = latch-gated non-verbose fallback decoded locally w/ embit, fail-closed txid re-bind; review MAJOR latch-gate fixed via fresh dispatch) → TCK-CHAINKIND-001 (d36a040 — "esplora" kind → "publicinfo", sole producer, nothing persisted) → TCK-FEE-006 (c99d4b1 — debugger: mempool `minimumFee` (=1, whole-sat) over-vetoed policy bids and the narration misnamed it "next block floor"; ORCHESTRATOR DECISION logged: policy bids floor at MAX(next-block feeRange[0], relay rail), minimumFee demoted to fallback-only; user's payload now bids 0.39/0.78/0.34) → TCK-RETRY-001 (5de495f — digit-free retry notes: 13 layer-3 strings + layer-2 pydantic remap; review MAJOR closed) → TCK-CLEANUP-001 (891b149 — warm-up bounded join + SWAP-001 dead-'deferred' sweep + WEB-020 planning-failure scan_error) → TCK-PUBLICBCAST-002 (26a0dd9 — stale slot lifecycle + explicit-404 evidence rule `ChainError.http_status==404`; review MAJOR "HTTP_STATUS ⊉ 404" fixed) → TCK-CHAT-005B (8a84c23 — spent-tx label hedge + schema-dump-order-follows-grammar adjudication w/ strict per-intent set-equality pin).
- **Skipped tests (user priority 2):** all five env-gated skips were RUN with gates set — #7 dump ✅ #6 HWI ✅ #5 bridge ✅ #3/#4 ❌ = VALID tests exposing the real ELECTRUM-001 bug (now fixed; re-run them green next suite). #1/#2 were already fixed by TCK-TEST-001. skipped-tests.md updated.
- **Ledger additions (user MW-16 round-4 notes, all ticketed):** TCK-WEB-030 (hostname→IP trust: green on LAN, privacy badge hidden when private — supersedes the `.local`=YELLOW decision per user guidance), TCK-WEB-031 (header "Wallet <hex>" left-aligned + page title + "Electrum:/Bitcoind: <host>" chip + balance button removed), TCK-WEB-032 (fp parity investigation: Sparrow+device both show 40DBB192; honest-verdict deliverable, no fp synthesis), TCK-CHAT-008 (first-connect incoming spam suppression), TCK-UTXO-005 (UTXO row rework: commas, icons, copy buttons, sats↔BTC toggle), TCK-CONS-003 (flexible consolidation phrasings), TCK-CHAT-009 (flexible UTXO/history filters + shared label normalizer + "KYC coins" case repro), TCK-FEE-007 (rate-ask loop on consolidation), TCK-HW-007 (show-on-device routing), TCK-ELECTRUM-002 (get_tx_status still always-verbose), TCK-CPFP-003 (parent-gone detector unification). Critique pass ran (10 defects found, all fixed in-ledger); part-10 serialization order pinned in TASKS.md.
- **AUTONOMOUS DECISIONS:** user ratified #1/#2/#5 (label-removal absence, coin_labels write-frozen, consolidation SLOW), gave guidance on #4 (→ WEB-030), default-ratified the rest; FEE-006 floor decision + CHAT-005B schema-follows-grammar adjudication logged in MANUAL-WORK §AUTONOMOUS DECISIONS.
- **Reviewer-routing correction:** one re-review was initially dispatched same-family (qwen/qwen) — caught by the reviewer itself, redone via deepseek `code-review` (verdict unchanged: APPROVE, zero code defects). Routing rule re-confirmed: coder/web-builder diffs → deepseek `code-review`; coder-light/debugger/designer diffs → qwen `code-review-qwen`.
- **NEXT QUEUE (suggested):** the user-facing MW-16 wave in serialization order — CHAT-009 → CONS-003 → FEE-007 → HW-007 → UTXO-005 → CHAT-008 → WEB-030 → WEB-032 → WEB-031 (plus parallel-safe ELECTRUM-002/CPFP-003) — then the user's MW-16 round 4 re-test (now covering the fee floor, cancel, scan speed, and public-electrum fixes).
- **Waiting on user:** MW-16 round 4 (checklist in MANUAL-WORK), MW-17 leftovers, MW-15 fee run (fee floor now honest — re-test "0.75 sat/vbyte" after FEE-007), MW-9 broadcast.
- **Venv used by children:** `/var/folders/7q/zywlh9nn6pn2bs8z1y1b44j80000gn/T/opencode/protocol_venv` (has pydantic, httpx, embit==0.8.0, hwi 3.2.0, llama-cpp-python, pytest, ruff). It survived the reboot and was used (Python 3.12.13). For a fresh machine: `python3 -m venv .venv && .venv/bin/pip install -e '.[dev]'` (requires Python ≥3.12 — PEP 695 syntax; friendly guard in evals/run_evals.py).

## 0. SESSION UPDATE 2026-09-07, part 2 (later session — read this first; part 1 below is history)

- **Head of queue now = the WEB UI plan (ADR-0024 + ADR-0022, commit `e04d839`).** TCK-WEB-001 (engine thread + queue pump) is next; TCK-WEB-002/003/004 follow. NEW AGENT: user added **web-builder** (`.opencode/agents/web-builder.md`, cspark/qwen3.8-flash-next) — owns ALL of `src/localwallet/ui/web/static/**`; server/engine py stays with coder. WEB-002 split coder(server)/web-builder(client) against the pinned contract: GET / static, GET /events SSE (X-Auth-Token header), POST /turn {text}, POST /action {utterance}, GET /state.
- **MW-4 DONE** (Jade signs end-to-end; broadcast deferred → MW-9; web manual matrix = MW-10). NO files are hands-off anymore. Jade findings drove TCK-HW-001 (requests dep + class-branched locked guidance), HW-002 (account-fp binding — master fp can never match an account zpub), HW-003 (PSBT derivation fields now carry master fp at sign time + change-output derivations emitted — fixes "no relevant inputs" + invisible change), HW-004 (signer selection config-authoritative; model's signer param advisory).
- **TCK-UX-002 DONE** (`bddc363`): brief card (To/Pay/Fee/From + one conditional slot), FLOW-REQUOTE (replacement from CREATED, commit-only-on-success, old ref inert), GATE-MERGE ("sign" joins CONFIRM whitelist, chained confirm→device-handoff in one turn; ADR-0013 amended); golden-031..034 + confirm-bypass-007. User notes for the next CLI run: new brief card + merged "sign" flow are live.
- **Also done:** TCK-ONB-002 (`d94328e`), TCK-SCAN-001 (`4ea9f2d`, fresh-wallet 81→41 calls), TCK-UTXO-001..004 design (`bec67fb`, docs/ux-utxo-notes-design.md — selection policy draft, tag model, settings), §9 sign-off on ADR-0023 copy.
- **Suite baseline: 1845 passed / 7 skipped**; evals fixture 34/34 + 24/24 exit 0; enforced gate record unchanged (26/53 = 49.1% exit 1, accepted). venv `.venv` Python 3.12.13 uv-managed — never recreate. `mycommand.txt`/`output.txt` at root = user's, leave alone.
- **Design points pinned at WEB-001 dispatch:** lint constant `WEB_SERVER_DIRS`; CLI world keeps between-turns tick (chain worker = web-world only); freshness result key `freshness` ('fresh'|'stale'); render red-team fixtures in `evals/redteam/render/`. ADR-0019 amendment (drafted in ADR-0024 decision 9) lands with WEB-001, retiring the `tests/test_watch_incoming.py` thread pin as planned work.
- **Watch-items from reviews:** refused-create burns a change index (pre-existing window); future transports must invoke confirm via full `_run_turn` (silent-confirm hazard — ADR-0024 mandates); conflict-guidance note not narrated by CLI `_print_sign_tx` on non-device_error paths (web reads result guidance).

## 0c6. SESSION UPDATE 2026-09-14/15, part 9 (history)

- **THE ENTIRE PART-8 QUEUE LANDED — 20 tickets + a CI flake fix, all committed, all reviewed (code-review on every slice, security-review on every money/chain/signer/protocol/privacy path). Baseline now 4381 passed / 5 skipped; evals 78 golden + 28 redteam + 8 render exit 0; store schema v6; registry still 15 intents; ruff/lint clean; HEAD 003dbdd.**
- **Order landed:** TCK-SWAP-001 (2a485fd, hot-swap installs immediately — the restart pain is gone; FEE-004 wording rider closed) → TCK-LABELS-UNIFY (a34148f, **schema v6 address_label_set** — address is the labeling unit, coin_labels folded by per-address union, table retained write-frozen) → CFG-004/005 (5d16866/6a2fbfa, chat-managed settings + live apply incl. two boot-snapshot freeze fixes) → WEB-020/023/027/022 (scan-error chip; private-IP-green w/ new `own_node_private` enum + kind badges back; **wallet_fingerprint header chip — HONESTY DEVIATION: the descriptor carries the ACCOUNT fp, master fp is unrecoverable watch-only, the ticket's device-parity copy was false and was replaced**; suggested-server chips) → TXID-001 → CHAT-002 (a084a3e, **MONEY RULING: consolidation default rung = SLOW** so the §3a copy is true) → CONS-002 → CHAT-005 (protocol lockstep filters) → CHAT-006 → **CI flake fix (9847f98 — ElectrumFixture.stop() race, see below)** → HW-005 B+D (b8b7cfb, show-on-device works: hwilib rejects zpub desc bodies, rebuild carries pubkey hex + client master fp) → WEB-029 → PUBLICBCAST-001 (b3c3206, consent-gated mempool.space fallback w/ electrum relayfee gate capability) → CHAT-007 → FIAT-004 (8f37000, "$45" bug = prompt-routing + retry-note digit transcription; engine-side conversion, ROUND_FLOOR) → LAUNCH-004 (01e86f8, warm-up + RLock KV-context hardening + compose-area loading) → DESCOPE-M4 (439bcbe, esplora wallet paths deleted — wallet backends electrum+bitcoind ONLY, 101 orphaned tests retired).
- **CI FAILURE (user-reported, root-caused, FIXED in 9847f98):** `test_connection_refused_retries_then_chain_error` flaked on ubuntu-latest — `ElectrumFixture.stop()` didn't join/wake the accept thread (on Linux `close()` does NOT wake a blocked `accept()`), so a racing connect got served the unscripted error envelope ("tip-height request rejected by the server") instead of ECONNREFUSED. Test-fixture race only — client error handling correct. Fix: `shutdown(SHUT_RDWR)` before `close()` + bounded join + assert-not-alive + a 20-connect hardening pin. CI should be green on the next push of this branch.
- **AUTONOMOUS DECISIONS (user review requested — details in MANUAL-WORK §AUTONOMOUS DECISIONS):** no label-removal surface (union-only); coin_labels retained write-frozen; `.local` hosts classify YELLOW (mDNS unauthenticated); consolidation defaults SLOW; PUBLICBCAST electrum relayfee coverage; HW-005 D1 pubkey-hex refinement; WEB-027 wallet_fingerprint rename; chaindouble test double.
- **Ledger-grade MINORs carried in rows (no action yet):** residual 546 retry-note mechanism (protocol-scope ticket candidate — digit-free retry notes); prompt budget ~68 chars headroom (14,932/15,000); PUBLICBCAST stale public_bcast_txid slot + 404-substring lag key; CHAT-005 spent-tx label under-inclusion silent; model_dump field order vs grammar order; chain/config.py "esplora" kind name (cosmetic, load-bearing); warm-up daemon unbounded join; WEB-020 planning-failure subclass.
- **NEXT QUEUE (suggested):** user's MW-16 round 4 live re-test (the big one — nearly everything they reported is now fixed), then the ledgered MINOR sweep, then WEB-014/015/017/018 static wave if desired.
- **Waiting on user:** MW-16 round 4 (refreshed checklist in MANUAL-WORK), MW-17 leftovers, MW-15 fee run, MW-9 broadcast (now with the public fallback available).

## 0c5. SESSION UPDATE 2026-09-13, part 8 (history)

- **PART-8 ADDENDUM (same day, user live-testing round):** FIVE more tickets landed — TCK-DIAG-004 (f8bb10d, `--minrelay` viewer on probe_backend_diag; review MAJOR bare-root URL fixed + re-review APPROVE), TCK-DIAG-005 (fb0349b, broadcast rejection classification: electrum rejection-as-result-string → `server-rejected`, JSON-RPC error → `rpc-error`, txid bind → `txid-bind-mismatch`, esplora siblings folded; fixed a latent fixture tuple-wiring that masked the H2 dialect), TCK-FEE-005 (6046889, **min-relay rail 1→0.1 sat/vB — the user fact-checked us against policy.h and was RIGHT**: `DEFAULT_MIN_RELAY_TX_FEE{100}` in master & v31.0; gate constant found in dust.py, rail-equality pin, BIP-125 increment stays 1 sat/vB deliberately), TCK-TEST-002 joins, plus the hot-swap debugger pass (root cause: settings edits SAVE immediately but the client swap is DEFERRED behind the in-flight scan — TCK-SWAP-001 ticketed w/ the cooperative-abort design; contract relaxation BLESSED, decision logged in MANUAL-WORK #7). **USER IDEA ticketed:** TCK-PUBLICBCAST-001 (broadcast the same signed tx via mempool.space POST /api/tx when our node rejects it as fee-too-low — explicit consent, fee-floor-gated offer, SEC-004 txid bind, status-lag honesty). **USER DECISION recorded:** TCK-LABELS-UNIFY — the address IS the labeling unit (one address = one key = one provenance; coins inherit; per-UTXO label = addition to the address's set; coin_labels migrates by per-address union) — implementation ticket ready, dispatch on user "go". New tickets also written from user LLM tests: TCK-CHAT-007 (receive+label dual action), TCK-FIAT-004 ("send $45" produced 546 sats — diagnose), TCK-LAUNCH-004 (startup warm-up generate + single loading bubble / compose-area indicator — user's spec replaces the diagnose-first PERF-001). **Baseline now 3814 passed / 5 skipped; HEAD 6046889.**
- **NEXT QUEUE (revised):** TCK-SWAP-001 (hot-swap abort — user's restart pain) → TCK-FEE-004-MINOR narration wording → TCK-CFG-004 → CFG-005 → TCK-WEB-020 → WEB-023 → WEB-027 → WEB-022 → TXID-001 → CHAT-002 → CONS-002 → CHAT-005/006 → HW-005 slices B/D → WEB-029 → LABELS-UNIFY (on "go") → PUBLICBCAST-001 → CHAT-007 → FIAT-004 → LAUNCH-004 → DESCOPE-M4.

- **ELEVEN tickets landed (all committed, all reviewed; suite 3575 → 3793/5):** TCK-CONS-001 (dbc9a30 — consolidation conversation: label roll-up → 1-coin-or-N → registry-number list → self_transfer consolidate, broadcast-time `consolidation` tag + "consolidated from N outputs", `_dispatch_code_self_turn` shared dispatcher), TCK-SCAN-BITCOIND-002 (3b69f04 — **debugger root cause: Core's getrawtransaction NEVER emits height/blockheight** (TxToJSON: blockhash/confirmations/time/blocktime only); the SCAN-BITCOIND-001 fixture fabricated the field, masking two fix attempts; scan-row heights + honest-None status; the user's startup-scan + watch failures are fixed), TCK-FIAT-003 (ce38c2e — EUR ask answers in EUR via a deterministic currency-word intercept, one-shot per turn, model still never authors currency; PLUS card Fee-line fiat), TCK-TEST-002 (35e2e2a — suite flake fixed: every web test joins the engine thread; 3× clean), TCK-WEB-021 (c28a394 — settings-pane stars rework per council), TCK-LABEL-001 (8f11643 — store schema **v5** `address_labels` + deterministic "label <bc1-addr> as <label>" intercept; ack quotes store truth), TCK-HW-006 (257da15 — probe-time wallet_match verdict reusing the HW-002 bind verbatim; "not the private key for this wallet" copy), TCK-WEB-028 (52fb4c1 — stuck-pending-bubble reconcile, failure-copy split, reconnect transition-only, QR Tab-trap, button disable-on-click), TCK-FEE-004 (41fc83d — **MAX(policy rung, node min-relay floor)** per the user's corrected spec; node floor from getmempoolinfo minrelaytxfee wired into the production estimator; explicit rates floored by the RELAY floor only — review MAJOR (publicinfo-only wiring) fixed via fresh dispatch + re-review APPROVE), TCK-DIAG-003 (28b10cd — broadcast failures emit `broadcast: send failed [class=… exc=… code=…]` to console+log, value-free), TCK-ONB-008 (253ef83 — chat creds: chat asks, Settings collects, `user:pass@host` refused value-free; NEVER creds in chat text per council).
- **USER NOTES TRIAGE (their session):** bitcoind block-height failure = root-caused + fixed (SCAN-BITCOIND-002); EUR = FIAT-003; fee-fiat folded into FIAT-003; "label bc1q… as KYC" = LABEL-001; "show on hardware wallet" = HW-005 slice B (still pending); broadcast debug = DIAG-003; min-relay = FEE-004 (MAX per user correction); config-via-chat = CFG-004/005 (pending); header ideas → council (below); chat creds → ONB-008.
- **UX COUNCIL verdicts on the user's three header ideas (AUTONOMOUS ARBITRATION — user review requested, see MANUAL-WORK §AUTONOMOUS DECISIONS):** (1) master fingerprint → BUILD (TCK-WEB-027: typed /state field + click-to-copy chip; qwen preferred settings-only, glm header — user's explicit "top header" direction wins); (2) HW "connected" chip → **REJECTED** as a persistent chip (stale-by-design trust lie — engine only knows device state at probe/sign; folded into WEB-018's device-wait rung with "at your last check" wording); (3) header host line → **REJECTED** (redundant + "Connected" verb collision; host appended to the own_node_remote privacy subline instead, folded into WEB-023). Plus chat-creds ruling → ONB-008. Council quick-wins → TCK-WEB-028 (landed).
- **New tickets written this session:** TCK-SCAN-BITCOIND-002 ✅, TCK-ONB-008 ✅, TCK-DIAG-003 ✅, TCK-FEE-004 ✅, TCK-CFG-004/005 (chat-managed config.json settings + live apply — critique-pinned conflict rule: chat-write = file write AND stored-rung delete), TCK-WEB-027 (fingerprint chip), TCK-WEB-028 ✅, TCK-WEB-029 (production stop() engine-join discrepancy), TCK-TEST-002 ✅.
- **NEXT QUEUE:** CFG-004 → CFG-005 → WEB-020 (scan-failure visibility) → WEB-023 (badge colors private-IP-green + host subline) → WEB-027 (fingerprint chip) → WEB-022 (electrum suggested-server chips) → TXID-001 → CHAT-002 → CONS-002 → CHAT-005/006 → HW-005 slices B/D → WEB-029 → DESCOPE-M4. Ledger-grade MINOR follow-ups ride their rows (FEE-004 congestion-note wording, ONB-008 ssl:// refusal wording, DIAG-003 wiring test, LABEL-001 repro-prose/_atomic, FIAT-003 _fee_fiat_pair dedup, WEB-028 first-submit race note).
- **Standing process rules unchanged (part 6):** small chunks, fresh fix dispatches, security-review on money paths, per-ticket commits, hunk-split shared files. NOTE: a child ran `git stash`/`pop` in the shared tree once (HW-006) — verified intact, but prefer serializing app.py/static slots to avoid it recurring.
- **Waiting on user:** resolved 2026-09-13 — all part-8 autonomous decisions RATIFIED (fee-fiat fold ✅, council creds agenda ✅, MAX ✅, council's header rejections ACCEPTED, fee-fiat note ✅); MW-16 leftovers → user re-verifies later; **TCK-LABELS-UNIFY = GO** (dispatch next session). MANUAL-WORK cleaned per user direction (autonomous-decisions section removed — outcomes folded into the MW sections; done checkmarks and the config.json section removed).

## 0c4. SESSION UPDATE 2026-09-13, part 7 (parts 1–6 are history)

- **RBF/CPFP wave COMPLETE + CHAT-001 landed (all committed, all reviewed):** TCK-RBF-005 (c6a02e3 — `_resolve_in_flight_outgoing` pinned resolver + lineage-aware tx_status), TCK-CPFP-001 (7e2cf40 — self_transfer mode `cpfp` + merge_coin, tx/cpfp.py child builder, registry still 14 then), TCK-RBF-004 (ea8e0c8 — bump conversation; review MAJOR "fee knob dropped across ask intercepts" FIXED via fresh dispatch), TCK-CPFP-002 (0f720dd — cpfp conversation: 3-option menu, Child-pays-for-parent hedge, evicted-parent class), TCK-CHAT-001 (2c3afe1 — store schema **v4** `address_registry`, **registry 15** w/ new `get_addresses` intent + additive `address_number` on get_balance/get_utxos, stable wallet-lifetime numbers, full-address-restated resolution, registry FACTS bounded-20).
- **Baseline: 3575 passed / 5 skipped; evals 60 golden + 27 redteam + 8 render exit 0; ruff/lint clean; registry = 15 intents; store schema v4.** Working tree clean at 72229a2.
- **Backfilled ledger rows (landed pre-session, were stale):** FEE-003 (8d21596+05146f5), UX-013/014 (cc871fc). Critique-pass folds landed as pinned: resolver API contract (RBF-005), cpfp handler guard rode RBF-004, selftransfer-bypass-001 re-pinned (CPFP-001).
- **New tickets from user MW-16/17 feedback (2026-09-13):** TCK-FIAT-003 (EUR ask returns USD values — diagnose display rung vs prompt mapping vs oracle cache), TCK-HW-006 (unlock-mismatch check: device fp vs descriptor account fp, honest "not the private key for this wallet" copy), TCK-WEB-023 AMENDED (electrum+bitcoind kind badges COME BACK colored private-vs-public — green on 192.168/10.x, yellow otherwise; partially reverses DESCOPE-M3B). MW-16 electrum/bitcoind/tls/probe-speed/autodetect + most MW-17 items ticked in MANUAL-WORK.
- **Ledger-grade MINORs carried in TASKS.md rows (no action yet):** RBF-005 multi-bump hedge ceiling (ponytail'd), CPFP-002 lone-coin menu-tail dead-end + dead merge_coin envelope rep + stuck-until-cancel copy, CHAT-001 (registry_number_for dead API, golden-count pin missing, facts wallet-binding), electrum 404-dialect hedge dependence (documented in RBF-004/CPFP-002 docstrings).
- **NEXT QUEUE:** CONS-001 (READY — CHAT-001 + UTXO-001 done) → CONS-002 (needs CHAT-002 pending) → then FIAT-003 / HW-006 / WEB-020 → WEB-021/022/023 → LABEL-001 → TXID-001 → CHAT-005/006 → WEB-017/018 → HW-005 slices B/D → DESCOPE-M4. Standing process rules unchanged (part 6): small chunks, fresh fix dispatches, security-review on money paths, per-ticket commits, hunk-split shared files.
- **Waiting on user:** MW-16 open items (creds UI, resync-tags, gap_limit decrease narration, first-query speed, model-absent path), MW-17 open items (EUR fix verification, badge return, HW-006 behavior), MW-15 live send re-check.

## 0c3. SESSION UPDATE 2026-09-12, part 6 (read this first; parts 1-5 are history)

- **De-scope landed (user redirection):** wallet backends = **electrum + bitcoind ONLY**; mempool.space demoted to public-info (fees/prices via `chain/publicinfo.py`); Esplora URLs unpersistable; kind badges removed; DESCOPE-M3A/M3B committed, **M4 (deletion/cleanup) still pending**.
- **Fee policy v2 (user spec, FEE-003):** MEDIUM = next-block feeRange[0] × 1.15, FASTER = 2×, SLOWER = 2nd-block floor; fractional centisat/vB end-to-end (`fee_rate_centisat_vb`, integer-exact).
- **RBF/CPFP wave IN FLIGHT:** store schema v3 (lineage: amount/fee-rate/first-seen/replaced_by_txid + derived BIP-125 superseded-retirement), pure replacement builder (tx/replacement.py — floor = old+max(old, relay), CHANGE_TRIM/FOLD/ADD_INPUT), `bump_fee` intent (**registry 14**), all committed + reviewed. **NEXT: TCK-RBF-005 (in-flight resolver + lineage-aware tx_status, app.py) → TCK-RBF-004 (bump conversation — CRITICAL contract: store `estimate_tx_vsize`, never a measured vsize; offer only CONFIRMED coins for the funding pick per BIP-125 rule 2) → CPFP-001/002 → CONS-001/002 (needs CHAT-001 numbers first).**
- **Hardware via chat (HW-005 slices A+C done):** probe/report/unlock + pre-sign device check + "sign with my hardware wallet" routing (code-stamped confirm = deliberate narrow gate extension w/ deny guard — security-reviewed). Slice B (show-address) + D (static button) pending.
- **Also landed:** chat-first onboarding (ONB-007, user's copy), bubble-order invariant (WEB-024), copy affordance (WEB-026), a11y turn announcements (WEB-019), fee v2, /details destinations + ref demotion, config.json at repo root, persona line, label/txid/filter/explorer tickets queued (LABEL-001, TXID-001, CHAT-005/006), settings wave queued (WEB-020/021/022/023), WEB-017/018.
- **Suite baseline: 3317 passed / 5 skipped; evals 52 golden + 27 redteam + 8 render, exit 0; ruff/lint clean; registry = 14 intents.** Working tree clean at b04f072/B204da6/36b925a + ledger.
- **Orchestrator process rules (user-directed, standing):** small chunks — ONE production module + its tests per coder ticket; mechanical conversions are separate sweep tickets; review-fix follow-ups go to FRESH dispatches (never resume the implementation session); children STOP-and-report adjacent work instead of widening; security-review on chain/tx/protocol/signer/privacy paths; per-ticket commits (hunk-split shared files; several splits already done — the patch-classify pattern works).
- **Waiting on user:** 🔥 MW-16 round 3 (backend re-test on Start9 — mempool shape fix + bitcoind scan fixes landed), MW-17/MW-15 refreshed checklists.

## 0c2. SESSION UPDATE 2026-09-10/11, part 5 (read this first; parts 1-4 are history)

- **EIGHT tickets done + committed (all reviews APPROVE):** TCK-UX-011 (da803b8 — /balance quick action stands down in web mode: instant cache answer + scan_pending + background ScanFlow kick, defer_scans transport carve-out, ADR-0022 amendment 2); TCK-WEB-010 (3f9c8a3 — per-bubble copy button); TCK-UX-012 (d4e802a — startup banner = separate bubbles via pump turn_end markers, watch-on silent + off pointer, interval-named watch failure + once-per-streak "watch: recovered.", kick-reality scan_pending + ScanFlow tail dedup); TCK-WEB-011 (1f8015b py + b838168 js — user_text event echoes your commands across tabs, FIFO dedupe, CLI byte-identical, watch narration closes its own turn); TCK-WEB-012 (b838168 — settings pane: distinct surface token, full-height, in-pane close + tiered Escape + focus management, legible badges, persistent privacy chip + visible subline, Enter-apply, load-retry); TCK-WEB-013 (1f8015b + 426c436 — effective chain URL always shown + privacy_mode trust badge green/yellow/red, empty field directly typeable + Cancel, first-run backend beat (pane stays open post-Connect), env-override honesty, 401→reload sentence on all pane POSTs); TCK-FIAT-002 (bbe76f1 — balances in usd/eur/gbp/cad/chf/aud/jpy, fail-closed ladder, currency-tagged oracle cache, USD byte-compat, model-never-authors-currency, ADR-0011 amendment, golden-040..042); TCK-BACKEND-003 (71048f1 — https Core RPC classified via new bitcoind+tls:// canonical scheme w/ tls_verify rung, /api-segment tolerance latched in the one Esplora join, value-free refusal hints — BOTH Start9 repros fixed + pinned); TCK-TX-SELF-001 (74ca47e — self_transfer intent registry 13: split big UTXO into N / consolidate dust, engine-derived deterministic plans, privacy-pool-respecting, dual-key gate unchanged, golden-043..047 + redteam bypass); TCK-PENDING-001 (narration enrichment: pending incoming/outgoing block on get_utxos, hedged digit-free ETA note, utxo-count phrasings pinned, golden-048/049).
- **UX COUNCIL ran on the settings pane (user request):** consensus + single-critic findings all folded into WEB-012/013 and landed. Rejected: watch-key copy restore (contradicts user's WEB-009 direction).
- **Suite baseline now: 2897 passed / 7 skipped**; evals 49/49 golden + 26/26 redteam + 8/8 render, exit 0; ruff + lint clean. Registry = 13 intents.
- **Ledger additions:** TCK-ADR-013-RECON (broadcast-gate ADR-text-vs-code reconciliation, INFO from TX-SELF-001 review), backlogs QR/CSV. LOW register: amount_usd USD-shaped bounds under display=jpy ceiling; stub $-parse quotes display currency (dev-only); /api latch may pin JSON-not-Esplora root (fail-closed); echo payload 2000-char cap vs 1MiB /turn; TX-SELF allocation-on-doomed-staging-path (create_tx parity).
- **Remaining ledger:** TCK-ADR-013-RECON, backlogs (QR, CSV), FIAT-002 LOWs if they bite, ONB-003 step-2 re-offers, TCK-WEB-006/P6-002/003 (user-gated). MW list: MW-16 re-test (BACKEND-003 fixes the two rejected URLs — run tools/probe_backend_diag.py per MANUAL-WORK), MW-17 relaunch verification, MW-15 live send.

### Part 5 addendum (2026-09-11, MW-16 round 1 user results — read with part 5)

- **MW-16 round 1:** electrum still green; bitcoind URL now CLASSIFIES (creds pane OK) but startup scan fails "utxo-scan request rejected by the server" (scantxoutset suspected — Core-22 floor or Start9 RPC-role permission); private mempool STILL rejected (both `192.168.0.25:56191` and `evil-start:56191` — note possible hostname typo vs `evil-star.local`; browser loads /api/v1/fees/mempool-blocks fine). Model download card/progress: user-verified WORKING. Gap-limit apply→rescan works (smaller-value semantics question raised). Cred matrix confirmed: bitcoind=yes, mempool/electrum=no.
- **New tickets:** TCK-DIAG-001 (value-free probe/scan error-class surfacing to console+log — user's explicit ask, also the support story), TCK-BACKEND-004 (root-cause both failures — BLOCKED on user's round-2 diag output, commands in MANUAL-WORK MW-16), TCK-GAP-001 (gap-limit decrease semantics + tradeoff narration).
- **USER PRIVACY DIRECTION (2026-09-11):** entering the zpub during onboarding appears to leak to public mempool.space immediately — TCK-PRIVACY-001 opened: audit EVERY outbound chain call pre-consent (scan is gated per ONB-006; suspects = fee/price oracle wrappers, watch, probe paths, web first-run beat not blocking engine-side), node info collected at the same time as zpub, and an EXPLICIT "use public server" consent button (closing the pane / asking a balance question must never imply consent). Zero-network pre-consent pins. Highest-priority privacy work — do after/with the MW-16 round-2 diag.
- User granted the orchestrator edit permissions (orchestrator.md `edit: allow`) — verify on session start.

## 0d. SESSION UPDATE 2026-09-10, part 4 (read this first; parts 1-3 are history)

- **User UX/copy wave DONE (4 tickets, all committed, security-review APPROVE ×2):** TCK-FIAT-001 (9c1fd2e — get_balance carries best-effort USD: handler keys usd_total_cents/btc_usd/rate_stale, card line "≈ $… · @ $…/BTC", prompt line maps fiat phrasings → get_balance, golden-038/039; registry still 12 intents, USD-only by user decision, EUR = follow-up on demand); TCK-UX-008 (b2f5f3b — CSS 3-dot turn-busy pulse, reduced-motion opt-out); TCK-UX-009+010 (ea620bd — host-named remote privacy banner, "Background watch: on. Change it in settings." backed by a REAL watch_interval_s settings rung (env > stored > default 60, 0..86400 fail-closed, requires_restart), "Loading local llm."/"Local llm fully loaded.", /state privacy_mode closed enum + colored header chip red/green/yellow per user decision (remote node = YELLOW)).
- **Suite baseline now: 2659 passed / 7 skipped**; evals 39/39 + 25/25 + 8/8 exit 0; ruff + lint clean.
- **Root cause found, fix deferred:** user's "/balance did nothing for minutes" = lazy inline first-scan inside the get_balance handler (app.py ~:1292) when no sync cursor + no startup scan in flight — minutes-class work serialized on the engine pump after the client echo. Candidate follow-up ticket TCK-UX-011 (async quick-action scan w/ progress) — NOT yet written.
- **Carried LOWs (cosmetic, ledgered in TASKS.md rows):** out-of-bounds watch-interval refusal names the env var even when the value rode the config-file rung; post-hot-swap shared-oracle invariant now RESTORED (was a FIAT review LOW, fixed in UX-009d).
- **Remaining ledger:** TCK-UX-011 (candidate, unwritten), ONB-003 step-2 re-offers, 3 documented LOW ceiling notes, TCK-WEB-006/P6-002/003 (hardware/user-gated), MW list (MW-16 backend live verification is the highest-value user action).

## 0c. SESSION UPDATE 2026-09-08/09, part 3 (read this first; parts 1-2 are history)

- **FIFTH WAVE (2026-09-09, continuing):** ONB-004 M2 (BitcoindClient — scantxoutset watch-only scan, auth matrix, capability floor, ca78020) + stale-gate LOW closed (identity-stable gate, mutation-proven); designer pass 2 applied (replace affordance live-verified, badge a11y, honest quickbar — 2818d77); ONB-004 M3 (autodetect + creds UX + cookie fix + designer rewords — cdcff81, review APPROVE w/ 2 LOW tracked). **ONB-004 COMPLETE: mempool-compatible, electrum ssl, and bitcoind backends all selectable with autodetect + creds.**
- **Suite baseline now: 2613 passed / 7 skipped** (fifth wave; evals green).
- **SIXTH slice (same day):** UTXO-003/004 + creds atomic apply (M3 review LOWs closed) — the UTXO tag feature is now end-to-end (tags → selection policy → card narration → settings). **Suite baseline now: 2630 passed / 7 skipped.**
- **Remaining ledger:** 3 LOW documented-ceiling corner cases (UTXO/creds review notes), ONB-003 step-2 re-offers, TCK-WEB-006 (optional frozen build), P6-002/003, MW list (MW-9/10/11/12/13/14/15 user actions).
- **FOURTH WAVE (2026-09-09, user UX push):** APP-LOG-001 (web-first output: narration SSE-only, console=errors, logs/launch-<timestamp>.log — 457fe14); DIST-004 (install.sh model state table: verified-present skips, .part resumes, corrupt re-downloads — c04d9f5); BACKEND-002 (chain client HOT-SWAP no-restart, probe-before-save, resync_now + POST /resync tags-survive, ssl:// accepted everywhere, backend_kind enum — d633e50, review APPROVE w/ LOW stale-gate follow-up tracked); WEB-009 (settings pane = only first-run surface, collapsed zpub, badges, Edit⇄Apply, Resync-now, BTC/USD quick bar — 4260513); LAUNCH-003 (background model preload + build lock — first query no longer slow; launch checksum — 7278e56).
- **Suite baseline at fourth wave: 2425 passed / 7 skipped** (evals green).
- **Remaining ledger:** ONB-004 M2 (bitcoind) + M3 (autodetect + creds UX), stale-gate LOW follow-up (BACKEND-002 review), designer pass on newest LABELS strings + watchkey-replace affordance decision, TCK-UTXO-003/004, TCK-WEB-006 (optional), P6-002/003, MW list.
- **THIRD WAVE (2026-09-09, user-driven):** token-necessity review (KEEP, ADR-0024 §6 note 3e66829); WEB-007 browser bootstrap deadlock fixed (GET / open w/ island — 71fbe50); UX-006 banner trim; ONB-005 /setup (95d44ce); stale-URL repair + debugger verdict (server not broken — 92631de); BACKEND-001 TLS ladder for Start9 self-signed certs (6734da4); ONB-006 scan-deferred-until-backend-choice (abe00ef); LAUNCH-001 web-first default + browser auto-open + /watchkey + fixed-port + favicon (d323f6b); UX-007 designer first-run spec + WEB-008 form dismissal/settings consolidation (1518fb9, f3c746a); ONB-004 adapter plan (docs/onb-004-backend-adapters-plan.md); LAUNCH-002 default-model + download Yes/No card + inline progress + quick-action bypass + watchkey replace (d03a4d1); ONB-004 M1 Electrum ssl adapter (d03a4d1).
- **Suite baseline now: 2376 passed / 7 skipped**; evals 37/37 + 25/25 + 8/8 exit 0.
- **Launch today:** bare `.venv/bin/python -m localwallet.ui.cli` → web UI + browser. Start9 private mempool: LOCALWALLET_CHAIN_BASE_URL=https://<host>:<port> + LOCALWALLET_TLS_VERIFY=false (or OS-trust the cert). Electrum: ssl:// URL via env/config-file (M1); /setup + autodetect + creds = M3 (next with M2 bitcoind).
- **Remaining ledger:** ONB-004 M2 (bitcoind scantxoutset) + M3 (autodetect + creds UX), ONB-003 step-2 re-offers (deferred), designer pass on newest LABELS strings, TCK-UTXO-003/004, TCK-WEB-006 (optional frozen build), P6-002/003, MW list (MANUAL-WORK.md).


- **SECOND WAVE (same session, later):** TCK-UTXO-001 (937a3dc, store schema v2 coin_labels + /label, review APPROVE), designer web-copy pass applied (9b6f805, docs/ux-web-copy.md), TCK-UTXO-002 (e07c171, tag-aware selection policy, review APPROVE — 2 LOW settings foot-guns → fold into UTXO-003), TCK-CFG-002 (68effa7, ~/.localwallet/config.json 4-rung ladder), TCK-ONB-003 (3696df7, full onboarding, review MEDIUMs fixed: check_backend collapse + real /blocks/0 shape), TCK-UX-001 marked ABSORBED by SCAN-003.
- **Suite baseline now: 2177 passed / 7 skipped**; evals 37/37 + 25/25 + 8/8 exit 0; ruff + lint clean.
- **Remaining ledger:** TCK-UTXO-003 (settings surface — fold UTXO-002's 2 LOW store items), TCK-UTXO-004 (narration/eval wiring for selection: From-clause, mix warning, re-quote tag-mix), TCK-WEB-006 (packaging), TCK-P6-002/003 (user hardware/decisions), MW-5/7/9/12/13/14/15 user actions (see MANUAL-WORK.md — MW-10/11 unblocked, MW-12..15 new).


- **WEB QUEUE COMPLETE + user follow-ups landed.** Done this session, all committed with per-ticket security reviews where required: TCK-WEB-002 client half (cf4df21), TCK-WEB-003 (ca2dbbf, combined transport review APPROVE), TCK-WEB-004 (a42bdaf), TCK-SCAN-002 ADR-0022 reconciliation (310b1d5), TCK-SCAN-003 non-blocking scan (fecb73f, review APPROVE + MEDIUM persist-merge fix), TCK-WEB-005 both halves (0f079b8 + 630b9dc, review APPROVE), TCK-PSBT-001 binary .psbt sibling (e7ab1d4, review APPROVE), TCK-FEE-001 fee floor-follower (2d343b7, review MEDIUM closed), TCK-FEE-002 explicit fee_rate_sat_vb schema extension (04b4d16), TCK-UX-004 ceiling ask (28d4a58), TCK-UX-005 rate display (0882626), TCK-DIST-001 curl installer (1ef8f67), TCK-DIST-002 landing-page staging in website/ (16aff89), TCK-DIST-003 open-source prep (96a4e59).
- **Suite baseline: 2177 passed / 7 skipped** (second wave; was 2038/7 mid-session); ruff clean; network lint exit 0; evals fixture 37/37 golden + 25/25 redteam + 8/8 render, exit 0; enforced model-mode gate record UNCHANGED (26/53 accepted — do not re-adjudicate).
- **USER ACTIONS pending:** publish to github.com/unsaltedbutter-ai/local-wallet (docs/publish.md has exact commands; secrets audit clean — decide on notible.local/192.168.1.50 scrub first); deploy website/ drop-ins to ~/unsaltedbutter/web (PM2/nginx steps in website/README.md; NOT verified against a live Next 16 build); MW-10 web manual matrix + MW-11 browser check now UNBLOCKED (web UI fully functional); MW-5/MW-7/MW-9 unchanged.
- **Next in ledger order:** TCK-UTXO-001 (store tags + /label — app.py free now) → TCK-UTXO-002 (selection) → TCK-CFG-002 → TCK-ONB-003; TCK-UX-003/UX-001 backlog; designer pass on web LABELS map + button labels (web-builder flagged). TCK-P6-002/003 need user hardware/decisions.
- **Fee behavior change to know:** FAST/MEDIUM/SLOW now floor-follow mempool (projected block bottoms + last-5-blocks floor + minimumFee; user's observed case bids 1 sat/vB, not 2); create_tx accepts explicit fee_rate_sat_vb 1–10000 (mutually exclusive with fee_target); "faster" at the top rung asks for a rate and the full loop works.
- **Watch-items carried:** refused-create burns a change index (pre-existing); confirm only via full _run_turn (ADR-0024); FeeSource unconsumed narration hook; STATE_TIMEOUT_S=120 fine while client polls /state only on demand.

## 0b. SESSION UPDATE 2026-09-07, part 1 (morning — history)

Committed this session, all gates green:
- **TCK-P6-001 DONE** (`a63e24e`): `_ENFORCE_PHASE6_GATE = True`; 17 new redteam fixtures (chain-injection, destructive-bypass, xpub-exfil → registry 23); CI-able gate pins `tests/test_run_evals_gate.py`; fuzz deep matrix.
- **TCK-ONB-001 DONE** (`0198c4a`): ADR-0023 first-run backend selection, REVISED TWICE per user UX direction — 5-step onboarding conversation (greeting+zpub ask → skippable node-privacy ask post-xpub → non-blocking load narration → load-complete → backend setup branch); copy drafts pending §9 sign-off; ONB-003 AC rewritten to match.
- **TCK-P6-004 DONE** (`db820fe` + runtime part in `46e80d9`): installed llama-cpp 0.3.35 GBNF parser rejects underscore rule names + multi-line rules — envelope.gbnf translated (machine-checked equivalence, 0/90 verdict diffs); drift pin now asserts REAL generate-time parse; fixed two latent local-runtime bugs (generate() returned dict not str; no max_tokens → 16-token truncation). Model mode now RUNS on the pinned GGUF.
- **TCK-AGT-001 DONE** (`46e80d9`): DEFAULT_TEMPERATURE 1.0 → 0.2 (grammar-constrained JSON = low-temp task; live evidence: user's send request escalated while identical prompt passed 5/5 isolated). Probe 3/3 clean.
- **TCK-CFG-002 opened** (pending): user-editable config file (gap_limit without env var) — user feature request.
- **MW-4 IN FLIGHT (user):** hands-off = `src/localwallet/app.py`, `src/localwallet/wallet/scan.py`, `docs/phase3-ac.md`, `docs/device-notes.md`. TCK-SCAN-001 is READY but held until the user reports MW-4 done (don't churn scan.py mid-run).
- **Next in order:** MW-4 report → TCK-SCAN-001 → TCK-SCAN-002 (ADR-0022) → TCK-SCAN-003 → TCK-CFG-002 / TCK-ONB-002/003 (ONB-003 touches app.py narration — after MW-4). TCK-P6-002 (packaging) also unblocked but needs user hardware (MW-8).

## 1. Current phase

**Phases 0–5 COMPLETE.** The security sweep (TCK-SEC-001..004) and the mainnet flip (TCK-MAIN-001..003, ADR-0021) have both landed. Phase 6: **TCK-P6-001 is now UNBLOCKED** — model bootstrap (MW-2) completed 2026-09-06 (weights pinned + `--check` verified), so the gate's sequencing note is satisfied. Was: **SEQUENCED BEHIND the model bootstrap** (`models/download_model.py --write-hash`) — the enforced ≥95% golden gate must not be adjudicated on ADR-0007 bridge results. The download is now **unblocked / live-verified working** (ungated unsloth mirrors per `df0ef64`; E2B URL verified 200 + GGUF magic + Range 206) once the sibling TCK-MODELS-002 lands (adds the E4B manifest entry + QAT q4_0 alternative).

## 2. Tickets

**Done (Phases 0–5 = 32 tickets, all committed, all with passing tests; security-review gate passed for every ticket touching `chain/`, `protocol/`, `tx/`, `signer/`) plus the security sweep + mainnet flip below:**
- Phase 0: TCK-P0-001…007 (scaffolding, protocol core, chain adapter, model runtime+grammar, agent runtime, walking skeleton, evals skeleton) + TCK-P0-008 (temporary remote-LLM bridge, ADR-0007) + TCK-P0-009 (envelope playground, `tools/envelope_playground.py`, loopback-only).
- Phase 1: TCK-P1-001 (SQLite store), P1-002 (wallet engine + ADR-0008/0009/0010), P1-003 (get_history/get_utxos/new_address intents), P1-004 (app wiring), P1-005 (evals→19), P1-006 (AC harness).
- Phase 2: TCK-P2-001 (fees+price oracle, ADR-0011), P2-002 (tx core: dust/selection/PSBT, ADR-0012), P2-003 (confirm gate + TxFlow, ADR-0013), P2-004 (send-flow wiring), P2-005 (evals→27 + 6 red-team), P2-006 (Sparrow harness).
- Phase 3: TCK-P3-001 (FilePsbtSigner, ADR-0014), P3-002 (signed-PSBT re-validation), P3-003 (HwiUsbSigner, ADR-0015), P3-004 (sign/broadcast/status intents + chain broadcast), P3-005 (lifecycle wiring), P3-006 (AC harness).
- Phase 4: TCK-P4-001 (node detect+doctor, scoped lint exception, ADR-0016), P4-002 (config-only backend switch, zero-public-calls guarantee, ADR-0018), P4-003 (node_status intent, registry 12, privacy-indicator flip, ADR-0017; also swept the `_CONFIRMED_LINE` narration fix — old footnote 2 resolved).
- Phase 5: TCK-P5-001 (watch_incoming tick-driven poller, dedup, time-since-block, ADR-0019 — decision: NO new intent, surfacing is deterministic), P5-002 (deterministic ETA narration-only, session memory R13, transcript scrub/export OQ14, ADR-0020).
- Security sweep **TCK-SEC-001..004** (commits `55f8d6d`/`f4d27b8`/`ce8603b`/`b582ab1`/`41dd321`): SEC-001 export redaction hardening (JSON-keyed amounts, 64-hex txids, BIP39-shaped seeds); SEC-002 absolute scan-window ceiling (1000/branch) + explicit truncation flag; SEC-002b surface scan-window truncation in narration; SEC-003 lint ban-list extension + dynamic-import detection; SEC-004 broadcast txid bound to embit-computed txid, PSBT sidecar size gate, 3-state privacy banner.
- Mainnet flip **TCK-MAIN-001..003** (commits `e353aec`/`861ca75`/`7d98854`, ADR-0021 — supersedes ADR-0004): MAIN-001 wallet layer mainnet-only (descriptor/derivation gates inverted, coin-type 0′, private-key refusals strengthened); MAIN-002 protocol/chain/signer/node mainnet-only (bc1 recipients, mempool.space main defaults, Chain.MAIN signer default, rpc 8332, price sentinel kept as degenerate path); MAIN-003 tx/app/evals/AC mainnet flip (bc1 change gate, mainnet fixtures, sparrow PSBT regenerated, AC docs mainnet). Suite 1677 passed / 6 skipped; evals 30/30 + 6/6.

**TCK-SEC-005 (absorbed into this doc):** intent registry = **12**; the three network-lint exceptions (confirmed from `tools/lint_network.py` — `src/localwallet/chain/**`, `src/localwallet/agent/remote_runtime.py` (ADR-0007 bridge), and `src/localwallet/node/**` (ADR-0016 localhost node detect/doctor)); uv.lock resolved (`5202297`); SEC sweep outcomes recorded (independent 4-lane sweep; finding class `D FIX-REQUIRED` → landed as TCK-SEC-001). Note: **TCK-MAIN-001..003 + SEC-004 are pending the retroactive security-review ticket TCK-SEC-006 (in flight).**

**In progress: NONE.** Working tree is clean; no ticket is half-done.

**Next (exact ids, in order):** TCK-P6-001 → TCK-P6-002 → TCK-P6-003, with the model-bootstrap sequencing note on P6-001.

**Security-review gate note:** the dedicated security-review subagent type was broken this session (stale provider model id `"aspark/GLM-5.3-Flash"`); reviews were run via the general subagent with a read-only reviewer prompt — all tickets here passed review (P4-001: 1 FIX + 2 NOTEs fixed/addressed; P4-002: approved, F-1+F-3 fixed, F-2 deferred to P4-003 which landed; P4-003: approved 7/7; P5-001: approved, NOTE-2+NOTE-1 fixed; P5-002: approved, redaction FIX + export-overwrite NOTE fixed).

### Files each next ticket owns (from TASKS.md)
- **TCK-P6-001** (eval expansion + full red-team suite): flip the ≥95% golden gate to ENFORCED in `evals/run_evals.py`; red-team set runs in CI-able pytest; results recorded. Note: run only after the pinned-GGUF bootstrap (`models/download_model.py --write-hash`) — bridge results are interim, ADR-0007.
- P5/P6 own their files per TASKS.md rows.

## 3. Decisions & constraints that are easy to lose

1. **Mainnet-only (ADR-0021, supersedes the original "testnet-only until Phase 6" plan).** Enforced at parse level (`wallet/descriptor.py`: testnet keys/addresses and all private keys refused) + structurally (`WalletDescriptor` cannot be testnet) + `HwiUsbSigner` defaults `chain=Chain.MAIN` + recipients must be bc1 mainnet scripts + testnet coin-type (1′) origins refused.
2. **Network I/O only in `chain/`** — lint-enforced (`tools/lint_network.py`, AST-based, tested). Exactly THREE exceptions: `src/localwallet/chain/**`, `src/localwallet/agent/remote_runtime.py` (ADR-0007 LLM bridge, `AGENT_LLM_TRANSPORT_FILES`), and `src/localwallet/node/**` (ADR-0016 localhost node detect/doctor, loopback-only).
3. **Dual-key confirm gate (ADR-0013):** `TxFlow.confirm()` requires the model's `confirm_tx` envelope AND a same-turn `GateDecision.CONFIRM` from the deterministic utterance classifier (whitelist-exact, in `tx/flow.py`). An LLM "yes" alone is structurally refused. Flow: IDLE→CREATED→CONFIRMED→SIGNED→BROADCAST (+CANCELLED/EXPIRED); `create` refused while pending; broadcast only from SIGNED; TTL 600 s.
4. **Revalidation gate (`tx/revalidate.py`):** the ONLY route to `mark_signed`/broadcast. 12 fail-closed checks incl. per-signature EC verification (BIP-143 via `psbt.sighash(i)`), positional output/fee exactness. Note: embit's `finalize_psbt` does NOT check pubkeys — do not "simplify" using it alone. High-S sigs are REFUSED by this build (stricter than BIP62 default policy — pinned by test).
5. **Broadcast POST is single-attempt, zero retries** (double-broadcast risk); failure keeps flow SIGNED (retryable via new POST). Response txid re-validated 64-lowercase-hex.
6. **Grammar↔schema↔prompt LOCKSTEP (ADR-0002):** any intent change updates `protocol/envelope.py` + `agent/grammar/envelope.gbnf` + `agent/prompt.py` TOGETHER, plus drift pins (`test_grammar_conformance.py` — a test-side GBNF matcher reads the real grammar file; loosening the grammar fails tests) and eval fixtures. Registry currently **12 intents**.
7. **FACTS injection pattern (the P2-004 lesson):** the model can only quote dispatcher-owned values injected as FACTS blocks per turn (`_flow_facts` in app.py: `pending_tx_ref`, `confirmed_tx_ref`, `signed_tx_ref`, `broadcast_txid`). Never assume the model can see terminal-only output like the confirmation card.
8. **ADR-0007 bridge is TEMPORARY:** remote OpenAI-compat runtime (`notible.local:8084/v1`, `mlx-community/gemma-4-e2b-it-4bit` last verified). Runtime of record = llama-cpp-python in-process (ADR-0011 is price policy; model runtime is **ADR-0001**). Removal criterion: pinned GGUF bootstrap (`models/download_model.py --write-hash`). **No eval conclusions about the pinned E2B may be drawn from bridge runs** — recorded interim scores only.
9. **Money math:** integers only for sats/fees/vsize (integer weights: 42 overhead / 272 per P2WPKH input / 4×(9+len) per output); dust = Bitcoin Core v28 `GetDustThreshold` verbatim (P2PKH 546, P2WPKH 294, P2WSH/P2TR 330, **P2SH-P2WPKH 540 — the 360 figure is folklore**, corrected in ADR-0012); vsize estimate ≥ actual (conservative, ±1 documented).
10. **RBF:** sequence `0xfffffffd` on every input (signaled); N4 = no bumping implemented in v1.
11. **Value-free errors:** no addresses/amounts/txids/keys in exception strings or error details (log-scrubbing invariant, PROJECT.md §7.8). Single documented exception: `InsufficientFundsError` carries needed/available as structured fields (user-facing UI, never logged).
12. **embit pinned `==0.8.0`** (money-path serialization; its API quirks are documented in code: `Transaction.txid()` is a method, no vsize property, `bytes(Script)` raises — use `script.data`). **hwi>=3.1** (3.2.0 verified; real API is `commands.signtx(client, psbt)`, client from `get_client(type, path, chain=Chain)`).
13. **Envelope version stays `v: 0`** — intent additions are backward-compatible extensions recorded in ADR-0002's per-phase sections. Adding an intent = enum + params model + grammar branch + prompt entry + drift pins + eval fixtures, all in one ticket.
14. **Python 3.12+** required repo-wide.

## 4. Tests that matter (and last result)

Last full run: **1677 passed / 6 skipped** (see §6 for the skips). Non-negotiable suites when touching their subsystems:
- `tests/test_protocol.py` — the model-trust boundary (accept/reject matrices, value-free errors, registry completeness = 12).
- `tests/test_grammar_conformance.py` — grammar drift pin (reads the real `.gbnf`; also pins llama.cpp parseability when the wheel is present).
- `tests/test_tx_flow.py` — dual-key confirm invariant (test: confirm without gate is refused even with matching tx_ref).
- `tests/test_tx_revalidate.py` — the tamper matrix (45 tests; every tamper names its check; value-free).
- `tests/test_wallet_scan.py` / `test_wallet_descriptor.py` — gap semantics, testnet gates, prefix matrix.
- `tests/test_e2e_skeleton.py` (~96) — REPL-level lifecycle incl. production-path FACTS quoting.
- `tests/test_phase1_ac.py` / `test_phase3_ac.py` — composite AC stories (offline).
- `tests/test_lint_network.py` — the network-isolation invariant itself.
- `python evals/run_evals.py` — fixture mode must stay exit 0 (30 golden + 6 red-team validated).
Every gate run by children in this session: green at commit time.

## 5. Known failures & what was already tried

No OPEN failures. Historical, all resolved:
- TCK-P0-002's first dispatch returned empty (no files) → re-dispatched, succeeded; lesson: verify child output with `git status`/glob before committing.
- User's `python` alias = 3.11 → raw PEP 695 SyntaxError → friendly version guard added to `evals/run_evals.py`, verified against the real 3.11 interpreter.
- mempool.space `/blocks/tip` returns a block LIST, not an integer (live divergence, found by spot-check) → tolerant-but-strict parsing (bare int OR non-empty list of {height}, max wins), live-verified (tip 150513).
- mempool.space `/v1/prices` (testnet4) returned sentinel `USD: -1` → oracle correctly fails closed → sats-only degrade. **Historical / by design for the degenerate path (TCK-MAIN-002):** mainnet prices no longer hit this sentinel path, but the fail-closed sentinel handling is kept.
- Fixture vpub was unfunded → live demos ended at "Insufficient funds: need N sats, have 0 sats" — expected. **RESOLVED via MW-3 (2026-09-06):** a FUNDED MAINNET wallet is now available, so the funded-wallet literal ACs (docs/phase1-ac.md, docs/phase3-ac.md) are runnable.
- **Known model-quality gaps (not code failures, interim bridge evals):** golden 21/27 (misses: empty-prompt→respond; out-of-range limit 200 drift; change-address phrasing; two flaky USD/quick phrasings that re-ran correctly; standalone "yes please"/"confirm the transaction" → confirm_tx with invented ref). Red-team 0/6: the model DOES emit `confirm_tx` on every bypass attempt — this is exactly why the structural gate exists; the gate itself is proven in `test_tx_flow.py`. Expected to improve with the real GGUF + enforced grammar (llama.cpp) — the MLX server accepts the `grammar` field but ignores it (verified by `root ::= "ZZZ"` probe).

**Follow-up register (non-blocking NOTEs from Phase 4–5 reviews):**
- P5-001: process-scoped dedup (`_seen`) — tx surfaced pre-restart re-surfaces after restart; address=None first-sighting txs never later surfaced (ADR-0019 documents).
- P5-001: DIR_SELF sweep narration may read as an incoming amount (cosmetic).
- P5-001: production probe runs a full `scan_wallet` per due poll — cheaper incremental probe or background thread with own connection is a possible follow-up.
- P5-002: context budget is turn-count-bounded, not char-bounded (pre-existing; oversized single turn can exceed the asserted budget; `loop.py` user_text cap 2000, envelope injection uncapped).
- Stray untracked `uv.lock` at repo root (created by a tool invocation, not part of the toolchain) — **RESOLVED:** uv.lock housekeeping landed in `5202297` (file no longer at repo root).

## 6. Skipped tests — full inventory and why

> **2026-09-11:** superseded by **`skipped-tests.md`** (repo root) — the authoritative 7-skip inventory with fix-or-remove verdicts. Two skips were env-unnecessary and now run (TCK-TEST-001, 998def2): baseline **2909+ passed / 5 skipped**. Substance below still accurate.

> **2026-09-11:** this section's inventory is superseded by **`skipped-tests.md`** (repo root) — the authoritative 7-skip inventory with fix-or-remove verdicts. Two of the skips were env-unnecessary and now run (TCK-TEST-001, 998def2): the baseline is **2909+ passed / 5 skipped** and rising. Substance below still accurate.

The "6 skipped" in the last run are ALL env-gated, by design (hermetic default suite):
1. **llama-gated agent tests** (`tests/test_agent_loop.py`, `tests/test_agent_prompt_context.py`, 1–3 depending on venv): real llama-cpp-python generation + `LlamaGrammar` parse of the grammar file. Skip when the wheel or a model file is absent. Unblocks: model bootstrap (deferred-run item 10). NOTE: the shared venv has the wheel, so these may show as passes there; on a fresh `.venv` before `pip install -e .` builds llama-cpp-python they skip.
2. **Live network E2E** (`tests/test_e2e_skeleton.py`, `LOCALWALLET_E2E_LIVE=1`): real mempool.space run. Skipped by default to keep the suite deterministic/offline.
3. **Phase 1 live AC** (`tests/test_phase1_ac.py`, `LOCALWALLET_E2E_LIVE=1` + `LOCALWALLET_AC_ZPUB`): needs a FUNDED MAINNET vpub — now available via MW-3 (2026-09-06); runnable.
4. **LLM bridge live smoke** (`tests/test_remote_runtime.py`, `LOCALWALLET_LLM_LIVE=1`): real endpoint call.
5. **HWI live** (`tests/test_signer_hwi.py`, `LOCALWALLET_HWI_LIVE=1`): real USB enumerate (no device in CI/sandbox).
Additionally, three things are NOT tests but are effectively deferred-run and must not be forgotten: the **literal AC procedures** (funded wallet → docs/phase1-ac.md; real device + on-chain broadcast → docs/phase3-ac.md), the **manual Sparrow import** (docs/sparrow-ac.md), and **model-mode eval runs** (manual, interim — fixture mode is the automated part). Test-debt note: `tests/test_envelope_playground.py` pins the playground's `--golden` report against a tmp 11-fixture subset because `tools/envelope_playground.py`'s matcher lacks the `intent_in` expectation type that `evals/run_evals.py` grew in P2-005 — harmless, but the playground matcher is behind the runner now.

## 7. Next Task prompt

> Next Task prompt (drafted 2026-09-06, post model bootstrap):
>
> "Read AGENTS.md, TASKS.md, HANDOFF.md, MANUAL-WORK.md. Execute the next ready ticket in TASKS.md order: **TCK-P6-001** (eval expansion + flip `_ENFORCE_PHASE6_GATE` in evals/run_evals.py — adjudicate on the PINNED GGUF now in models/bin, not the ADR-0007 bridge; every protocol/prompt change ships an eval run). In parallel-eligible follow-ups behind it: TCK-SCAN-001 (utxo-skip), TCK-SCAN-002 (ADR-0022 scan-freshness), TCK-ONB-001 (ADR-0023 onboarding) — see queue notes in TASKS.md. The user is working MW-4 (live device AC, docs/phase3-ac.md) and will report MW-ids as they complete — update MANUAL-WORK.md + ledger on report, do not edit docs/phase3-ac.md or the narration paths (app.py/scan.py UX-001 surface) while that live run is in flight. Gates: .venv is Python 3.12.13 (3.14 breaks hwilib/protobuf — do not recreate on 3.14); venv managed with uv (`uv pip install …`); full suite baseline 1695 passed / 6 skipped."

## 8. Actions for You

1. **MW-2 — model download (DONE 2026-09-06).** E2B Q4_K_M pinned + `--check` green → **TCK-P6-001 unblocked**. Optional later: download E4B + QAT entries for the R12 comparison (commands in models/MODELS.md).
2. **MW-3 — fund a MAINNET wallet (DONE 2026-09-06).** The literal Phase 1 AC (docs/phase1-ac.md live cross-check) is now runnable; real send-flow demos are unlocked.
3. **MW-4+ — available now:** MW-4 hardware-wallet live AC (docs/phase3-ac.md), MW-5 Sparrow import AC (docs/sparrow-ac.md), MW-6 model-mode eval record (after MW-2), MW-7 post-SEC-001 export spot-check, MW-8 Phase 6 packaging prerequisites (Apple Developer account + Windows box, later).
4. **Full manual checklist:** see MANUAL-WORK.md (authoritative for MW-id status).

No sudo needed for anything in-repo: `python3 -m venv .venv && .venv/bin/pip install -e '.[dev]'`, then `.venv/bin/python -m localwallet.ui.cli --stub-llm` (no model needed) or with the bridge env vars (`LOCALWALLET_LLM_BASE_URL=http://notible.local:8084/v1`, `LOCALWALLET_LLM_MODEL=mlx-community/gemma-4-e2b-it-4bit`) for real E2B inference. For fast scans in dev, set `LOCALWALLET_GAP_LIMIT=2` (overrides the DB `gap_limit` setting; default 20 — a too-small gap can miss allocated-but-unused addresses; widen + rescan per ADR-0009).

## 9. Two honest footnotes (carried from the phase reports)

> Both footnotes are now **RESOLVED**; kept for the record.

1. **The golden eval gate is informational until TCK-P6-001** flips it to enforced (≥95%). **→ RESOLVED:** TCK-P6-001 (which now sits next) will enforce it. Interim bridge scores are recorded in TASKS.md deferred-run items (P1: 10/11 → 9/11 on a second run — nondeterministic at temp 1.0; P2: 21/27 golden, red-team 0/6 with the structural gate holding). The pinned-GGUF eval record is still pending the model download; bridge runs (MLX 4-bit, grammar field accepted-but-ignored by the server) are explicitly NOT the record per ADR-0007.
2. **One cosmetic string is stale:** `_CONFIRMED_LINE` in app.py still says the signed-transaction step "arrives in Phase 3" — it has now arrived (sign_tx exists and works). Trivial narration-wording fix, deliberately left untouched to avoid breaking pinned test assertions mid-phase; sweep it into the next ticket that touches narration (P4-003's banner work is a natural home). **→ RESOLVED:** swept into TCK-P4-003's narration/banner work.
